
> 一篇面向研究者与工程师的综述。核心论点：MoE与MMoE共享同一套机制（专家 + 门控），但服务于两个互不相干的动机。一边是"每个token该算多少"，一边是"每个任务该共享多少"。

# 一、引言

在2026年的文献里，"MoE"这三个字母已经严重过载。它可能指1991年那个由门控加权若干子网络的原始模型，可能指Transformer中替换前馈层的稀疏路由模块，也可能在推荐系统论文里泛指任何"多专家 + 门控"的多任务结构。

更麻烦的是MMoE。多任务学习社区用它指Google 2018年的Multi-gate Mixture-of-Experts，2023年底起一批多模态工作也用它指Multimodal MoE，两者除了共用"专家"这个词以外没有技术交集。

命名混乱会直接导致判断错误。工程讨论里最常听到的一句是"MMoE是MoE的多任务版本，所以大模型里的MoE也是为多任务服务的"。这个推论完全不成立。今天的大模型在训练时只有一个目标函数，整个过程跟多任务没有关系。

另一句常见的是"做多模态就要用MMoE"。多模态模型里用的是模态感知的稀疏路由，MMoE那个"每个任务配一个独立门控"的标志性特征，在主流多模态模型中并不存在。

回到源头，1991年的混合专家思想在此后三十余年里分化成两条几乎不通信的技术路线。

稀疏路线把"专家"当作容量单元，只激活其中一小部分，让参数量与计算量脱钩。它要回答的是：给定算力预算，模型能装下多少知识。这条路2017年后加速，2021年进入Transformer，到2025年成为开源大模型的默认架构，DeepSeek V3、Llama 4、Kimi K2一个不落。

多任务路线把"专家"当作共享单元，让不同任务用不同权重组合同一批专家。它要回答的是：几个训练目标之间该共享多少表示。这条路以MMoE为起点，落在推荐、广告、搜索的多目标排序里，至今几乎没有走向稀疏化，专家数常年停在十几个。

两条路线的机制形式几乎一样，动机、超参数量级、失效模式、评价标准全都不同。把它们混为一谈，是绝大多数误解的根源。

下面第二章讲共同起点，第三、四章分别展开两条路线，第五章做对比并处理多模态这条第三支流，第六章讲工程实践，第七章列开放问题。只想搞清缩写关系的读者可以直接跳到5.3节的对照表。

# 二、共同起点：条件计算的思想

## 2.1 1991 年的原始形式

Jacobs、Jordan、Nowlan与Hinton在1991年的《Adaptive Mixtures of Local Experts》中提出了混合专家的原始形式。给定输入 $x$，一组专家网络 $f_1, \dots, f_n$ 各自产生输出，一个门控网络产生归一化权重，最终输出是加权和：

$$ g(x) = \mathrm{softmax}(W_g x), \qquad y = \sum_{i=1}^{n} g_i(x) \cdot f_i(x) $$

这个式子本身平淡无奇，关键在于训练时发生了什么。论文的核心洞察是关于误差信号的分配方式。如果损失函数写成"加权和与目标之间的误差"，那么各个专家会被鼓励去互相配合、共同拟合残差，结果是所有专家都变成通才，谁也不专精。作者的做法是把损失改写为混合形式：门控权重被解释为专家被选中的后验概率，损失变成各专家误差的加权混合。这样一来，专家之间形成竞争关系：某个专家在某类输入上表现好，门控就给它更高的权重，它得到的梯度也更大，于是它在这类输入上进一步变好。这是一个正反馈过程，最终导致输入空间被隐式地划分给不同专家。

换句话说，MoE从第一天起就是一种**输入依赖的分工机制**，跟集成学习是两回事。门控在做的是分配，加权平均只是它的外观。

## 2.2 与集成学习的本质区别

这个区别值得展开，因为它直接决定了后来两条路线的可能性。

集成方法如bagging和boosting中，所有基学习器都参与每一次预测。集成的收益来自方差削减或偏差修正，代价是推理成本随成员数量线性增长。基学习器之间的差异来自数据扰动或训练顺序，而不是来自输入。

MoE中，参与程度由输入决定。理想情况下门控的输出是接近one-hot的，此时只有少数专家实质性地参与。这带来一个集成方法没有的性质：模型的总容量与单次前向的计算量可以解耦。1991年的实现并没有利用这个性质（所有专家都真的被计算了），但性质本身已经存在于公式里。三十年后稀疏路线所做的，本质上就是把这个理论上的解耦变成工程上的现实。

多任务路线走的是另一个方向。它保留了稠密计算，但注意到"分配"这件事可以由外部条件而非仅由输入来引导。如果门控的输入里包含任务身份，或者干脆每个任务配一个门，那么"哪些专家服务哪些任务"就变成了可学习的量。这同样是1991年公式的直接推论，只是取用了它的另一半含义。

## 2.3 分层混合与 EM 视角

Jordan与Jacobs在1994年的《Hierarchical Mixtures of Experts and the EM Algorithm》中把结构推广为树状：门控网络分层排布，每一层做一次软划分，叶子节点是专家。这给出了一个漂亮的概率解释：整个模型是一个条件混合模型，门控是隐变量的后验，可以用EM算法训练：E步计算每个样本归属各专家的责任度，M步分别更新专家与门控。

这个视角在今天仍有价值，主要体现在两处。一是它解释了为什么MoE的训练天然存在不稳定性：责任度分配与专家质量之间的耦合是一个鸡生蛋问题，初期的随机偏差会被正反馈放大，这正是后来"专家坍塌"现象的理论根源。二是分层门控的思想在现代实现中以另一种形式复活了。DeepSeek-V3的节点受限路由（node-limited routing）本质上是一个两级选择：先选节点，再在节点内选专家，虽然动机是通信开销而非概率建模。

## 2.4 为什么沉寂了二十年

从1994到2017，混合专家在主流深度学习中基本消失。原因是多方面的，值得列清楚，因为每一条的反面正好构成了2017年之后复兴的条件。

其一，稠密实现省不下算力。既然所有专家都要计算，MoE相比一个同等参数量的单一网络没有效率优势，只有额外的门控开销和训练麻烦。稀疏化的工程实现（尤其是在GPU上的高效稀疏矩阵乘）当时并不具备。

其二，门控坍塌难以控制。在没有辅助约束的情况下，正反馈机制经常导致少数专家吞掉绝大部分输入，其余专家几乎不接收梯度，退化为死参数。这个问题直到2017年引入显式的负载均衡损失才得到实用级别的缓解。

其三，数据规模不足。MoE的收益在于让不同专家覆盖数据分布的不同区域，这要求数据本身足够多样且量足够大。在中小规模数据集上，划分带来的样本效率损失往往超过专精带来的收益。

其四，缺少硬件与并行动机。MoE的现代价值很大程度上来自它天然适配模型并行，专家可以分散到不同设备上。在单卡训练是常态的年代，这个优势无处发挥。

其五，注意力机制尚未成为主干。MoE在Transformer中最自然的落点是前馈层，而前馈层占据模型参数大头这一事实要等到Transformer成为标准架构之后才成立。

这五条在2017年之后逐一被翻转：Transformer成为主干，前馈层成为参数大头，大规模分布式训练成为常态，数据量进入万亿token级，负载均衡损失被设计出来，稀疏计算的内核实现逐步成熟。混合专家的复兴来自这五个条件同时到位，没有哪一个单点突破可以独揽功劳。

# 三、分支一：稀疏 MoE 与大模型扩容

## 3.1 2017 年的转折：稀疏门控

Shazeer等人在2017年的《Outrageously Large Neural Networks: The Sparsely-Gated Mixture-of-Experts Layer》是整条稀疏路线的起点。这篇论文做了三件事，每一件都成为后续工作的默认前提。

第一件是把门控稀疏化。做法是在softmax之前加入可学习的高斯噪声，然后只保留top-k个最大值，其余置为负无穷：

$$ H(x) = W_g x + \epsilon \cdot \mathrm{softplus}(W_{\mathrm{noise}} x), \qquad g(x) = \mathrm{softmax}(\mathrm{TopK}(H(x), k)) $$

噪声项的作用是提供探索，让排名靠后的专家有机会被选中并获得梯度，缓解正反馈坍塌。top-k中的k取2或4，使得每个样本只需计算少数专家。

第二件是引入辅助损失来控制负载。论文实际用了两个量：importance是一个批次内各专家收到的门控权重之和，load是各专家被选中的期望次数。对两者分别施加变异系数的平方作为惩罚项。这个设计的逻辑值得注意。importance是可微的，但只约束权重大小。load更接近真实的计算负载，但需要用噪声分布的解析形式来构造可微的替代。后续所有负载均衡方案都是在这个框架上的变体。

第三件是把模型规模推到了当时不可思议的量级：在LSTM语言模型上堆到1370亿参数，而单样本计算量只相当于一个小得多的模型。这个数字在今天不算大，但它第一次在实证层面确认了参数量与计算量可以脱钩，并且脱钩之后模型质量确实继续提升。

从观念史的角度看，这篇论文改变的是人们对"模型大小"的定义。在它之前，参数量和算力成本是同一件事的两种说法。在它之后，两者成为需要分别核算的两个预算。

## 3.2 路由器设计的谱系

路由器是稀疏MoE中变化最丰富的部件。把主要设计放在一起看，可以整理出几条相互对偶的思路。

**token choice与expert choice。** 标准做法是token choice：每个token挑选它最想去的k个专家。这个方向的固有问题是负载不可控，多个token可能同时涌向同一专家，超出容量的部分只能被丢弃或绕过。Expert Choice路由（Zhou等，2022）把方向反过来：每个专家挑选它最想处理的固定数量的token。这样负载天然完美均衡，不需要辅助损失，代价是某些token可能不被任何专家选中，而另一些可能被多个专家重复处理，即每个token的实际计算量不再固定。两种方案是同一个二部图匹配问题的两个方向，各自放弃了一侧的保证。

**确定性路由。** 既然学习路由带来这么多麻烦，能否干脆不学？Hash Layers（Roller等，2021）用token id的哈希值直接决定专家归属，负载均衡由哈希函数保证，无需任何辅助损失。效果并不差，这个结果本身对"路由学到了语义分工"的叙事构成了不小的挑战。BASE Layers（Lewis等，2021）走另一条路：把token到专家的分配显式建模为线性分配问题，在每个批次内求解一个平衡最优传输，保证严格均衡。这类方法的代价是求解开销和对批次组成的依赖。

**稠密化的回归。** Soft MoE（Puigcerver等，2023）放弃了离散选择。它为每个专家分配若干slot，每个slot的输入是全部token的一个加权平均，输出再加权分发回各token。这样一切都是可微的稠密操作，没有token丢弃，没有负载均衡问题，训练极其稳定。代价是它不再是稀疏计算。计算量与专家数相关，而且由于每个slot混合了全部token，它无法用于自回归解码这类需要因果性的场景。Soft MoE在视觉领域表现良好，在语言模型上则受限。有意思的是，它在形式上反而更接近1991年的稠密原始版本，也更接近多任务路线的做法。

**无辅助损失路线。** 辅助损失有一个内在矛盾：它与主损失的目标不完全一致，权重过大会伤害模型质量，过小则均衡失效，需要仔细调参。DeepSeek-V3提出的做法是取消辅助损失，改为给每个专家的路由分数加一个可学习的偏置项，训练中根据近期负载动态调整这个偏置，超载的专家偏置下调，欠载的上调。偏置只影响选择，不参与最终的权重计算，因此不会污染梯度。这个方案在实践中被证明既能维持均衡又不损害质量，现已被多个后续模型采用。

**稳定性相关的路由技巧。** ST-MoE（Zoph等，2022）系统研究了MoE的训练不稳定问题，提出了router z-loss，对路由器logits的对数配分函数施加惩罚，抑制logits数值爆炸。这类"数值卫生"措施看起来琐碎，但在大规模训练中往往是能否跑完的关键。同类做法还包括路由器强制使用float32、减小专家初始化尺度、对专家使用更高的dropout率。

## 3.3 进入 Transformer：GShard 与 Switch

GShard（Lepikhin等，2020）把MoE层正式接入Transformer，训练出6000亿参数的多语言翻译模型。它确立了几个至今沿用的工程约定。

其一是放置策略：隔层替换前馈层，兼顾容量与通信开销，而不是每层都换。其二是容量因子（capacity factor）：为每个专家预设一个固定的token容量上限，等于批次平均负载乘以一个大于1的系数。超出容量的token被丢弃，直接走残差连接。这个设计的意义在于让张量形状静态可知，从而能编译成高效的并行程序，代价是引入了token丢弃这一质量损失来源。其三是随机次选：top-2中的第二个专家按门控权重的比例随机决定是否启用，进一步降低平均计算量。

Switch Transformer（Fedus等，2021）做了一个激进的简化：k取1。在Switch之前，社区担心top-1会导致门控无法接收有效的比较信号，因为没有第二个专家来构成对比。论文的实证结果推翻了这个直觉。top-1不仅可行，而且在同等算力下比top-2更优，因为省下的计算可以用于更多的专家或更多的训练步数。Switch还把规模推到1.6万亿参数，并给出了一套完整的稳定性配方：选择性精度（路由器用float32，其余用bfloat16）、更小的初始化方差、专家层专用的更高dropout。

这两篇工作之后，"MoE层 = 一组前馈专家 + 一个路由器 + 容量约束 + 辅助均衡损失"成为标准模板，后续的差异主要在路由细节、专家粒度和并行策略上。

## 3.4 四个绕不开的工程问题

**负载均衡与专家坍塌。** 这是最根本的问题，前面已多次提到。需要补充的是它在不同阶段的表现形式不同：训练初期表现为少数专家吞掉多数token。训练中后期可能表现为更隐蔽的不均衡，比如某些专家只接收标点符号一类低信息token，虽然计数上均衡但实际贡献不均衡。因此仅看负载方差不足以判断路由健康度。

**路由不稳定与训练发散。** 路由是离散选择，它对参数的微小变化可以产生跳跃式响应。当某个token的归属在相邻步之间反复切换时，两个专家收到的都是不连续的数据分布，容易造成损失尖峰。混合精度训练会放大这个问题，因为logits的微小舍入误差就足以改变argmax的结果。这是路由器坚持用float32的直接原因。

**专家并行下的all-to-all通信。** 专家分布在不同设备上，而token的归属由数据决定，因此每个MoE层都需要两次全局的all-to-all：一次把token送到对应专家所在设备，一次把结果送回。这两次通信在大规模集群上可以占到单层耗时的相当比例，且对网络拓扑高度敏感。这催生了一系列拓扑感知的路由约束，比如限制每个token最多跨越几个节点。

**显存按总参数计费。** 稀疏MoE省的是计算，不是存储。所有专家的权重都必须驻留在集群显存中（推理时也是如此，除非做offload），优化器状态更是按总参数量的数倍计算。这导致了一个常被忽视的成本结构：一个1.6T总参数、49B激活的模型，其推理算力接近49B稠密模型，但显存需求接近1.6T稠密模型。对自托管用户来说，后者才是真正的门槛。

## 3.5 结构层面的演进

2024年之后，稀疏MoE的结构设计出现了几个方向性的改动，其中影响最大的两个都来自DeepSeekMoE。

**细粒度专家切分。** 把每个专家的隐层维度缩小，同时成比例增加专家数量，使总参数和激活参数都不变，但激活的专家个数增加。直觉上，这提升了专家组合的表达能力：从N选2的组合数变成4N选8，可表示的"知识组合"数量呈组合爆炸式增长。经验上这个改动几乎免费提升质量，代价是路由器规模变大、通信的消息数变多。

**常驻共享专家。** 保留少量始终激活的专家，与路由专家并行工作。动机是把跨领域的通用知识集中到共享专家中，减少路由专家之间的冗余，否则每个专家都得独立学一遍基础语法。这个设计后来被广泛采用，比如同期的Qwen1.5-MoE就采用了4个常驻共享专家加60个路由专家（每次激活4个）的配置，总参数14.3B、激活2.7B，性能接近稠密的Qwen1.5-7B而训练资源减少约四分之三。

"共享 + 专属"这个模式与后文第四章PLE的核心设计在结构上高度同构，但两者是在完全独立的社区中、出于完全不同的动机被发明出来的。这一点将在5.2节讨论。

**稠密模型的upcycling。** 从零训练MoE成本高昂，一个实用替代是把已有的稠密模型改造为MoE：复制前馈层权重作为多个专家的初始化，再继续训练。Sparse Upcycling（Komatsuzaki等，2022）系统研究了这个路径。它的主要风险是专家之间初始完全相同，容易在后续训练中保持高度冗余，专精程度不足。缓解手段包括对副本加噪、使用不同数据子集做初期分化、以及只upcycle部分层。

**MoE层的放置。** 早期的隔层替换是一个粗糙的启发式。后来的实践普遍倾向于让浅层保持稠密（浅层负责通用的低级特征，专精收益小，且路由不稳定影响会向上传播），中深层使用MoE。DeepSeekMoE系列即是最初若干层保持稠密前馈、其余层全部替换，到DeepSeek-V3是前三层稠密、第4到61层全部换成MoE。

## 3.6 2024–2026 年的现状

到2026年中，这条路线基本上赢得了大模型架构之争。2024年还是稠密与稀疏并存，到2025年，稀疏MoE已经拿下几乎所有重要的开源权重发布，Llama 4、Qwen3、Kimi K2、GLM-4.5、gpt-oss无一例外。下面是截至2026年中期的一组代表性规模（总参数 / 每token激活参数）：

|模型|总参数|激活参数|稀疏比|
|---|---|---|---|
|Mixtral 8×7B（2024）|约47B|约13B|约3.6×|
|DeepSeek V3（2024）|671B|37B|约18×|
|Llama 4 Maverick|400B|17B|约24×|
|Mistral Large 3|675B|41B|约16×|
|GLM-5.2|744B|40B|约19×|
|Qwen 3.5|397B|17B|约23×|
|Kimi K2.6|约1T|32B|约31×|
|DeepSeek V4-Pro|1.6T|49B|约33×|
|Qwen3-Coder-Next|80B|3B|约27×|

（数字取自各模型公开发布的说明，为便于比较做了统一整理，不同来源对同一模型的口径偶有差异。）

这张表里有几个值得注意的趋势。一是稀疏比在持续上升，从Mixtral时代的3–4倍升到20–30倍以上，说明社区对"激活多少才够"的判断在不断放宽。二是稀疏比高的极端案例出现在小模型上，80B总参数配3B激活，意味着MoE不再只是千亿以上模型的专利，也成了让消费级硬件跑动强模型的手段。三是成本模型已经形成共识：总参数量决定显存下限，激活参数量决定推理算力，两者需要分开核算。

另一个趋势是MoE与其他效率技术的叠加。稀疏注意力、多头潜在注意力（MLA）、线性注意力或混合注意力被普遍与MoE组合使用，因为两者优化的是不同瓶颈：MoE削减前馈层的计算，注意力改造削减序列长度带来的开销。在长上下文场景下，只做其中一项收益有限。

## 3.7 scaling law 视角

稀疏MoE的一个理论问题是：一个N总参数、A激活参数的MoE相当于多大的稠密模型？这个"等效稠密参数"没有简洁答案，但有几条经验规律。

Clark等人2022年的《Unified Scaling Laws for Routed Language Models》给出了较早的系统研究，结论之一是路由带来的收益随模型规模增大而递减。在小模型上MoE相对稠密的优势明显，随着规模上升优势收窄，并且存在一个专家数量的收益饱和点。这个结论在当时被解读为对MoE前景的警告。

后续工作对此有修正。Krajewski等人2024年关于细粒度MoE的scaling law指出，专家粒度是一个此前被忽略的独立维度：在固定总参数与激活参数的前提下，把专家切得更细能持续改善损失，且此前观察到的"收益饱和"部分源于粒度维度未被优化。这一定程度上解释了为什么2024年之后的实际稀疏比能推到远高于早期预期的水平。

从实用角度看，几条经验值得记住。第一，在算力受限、显存充裕的场景下MoE几乎总是划算，反之则不然。第二，稀疏度不能无限提高，激活参数过小时模型的推理与组合能力会明显退化，即使总参数很大。第三，MoE在预训练损失上的优势不总能完整传导到下游任务，尤其是需要多步推理的任务，微调阶段的表现有时不如同等激活量的稠密模型。ST-MoE专门讨论过MoE微调时的过拟合倾向，缓解手段包括只微调非专家参数。

# 四、分支二：多任务学习与 MMoE

## 4.1 多任务学习的结构困境

多任务学习的标准做法长期是shared-bottom：底层若干网络层被所有任务共享，上面为每个任务接一个独立的塔（tower）。这个结构的合理性来自一个假设：不同任务需要的低层表示大体相同，共享既能省参数又能起到相互正则化的作用。

在任务高度相关时这个假设成立，共享确实带来正向迁移。但当任务相关性较低时会出现负迁移（negative transfer）：几个任务在共享参数上产生方向冲突的梯度，参数在互相拉扯中收敛到一个对谁都不理想的折中点，多任务模型的表现反而不如各任务单独训练。

这个现象的机制可以从几个层面理解。梯度层面，共享参数收到的更新是各任务梯度之和，当梯度之间夹角大于九十度时，合成方向对某个任务而言是负向的。表示层面，不同任务需要的特征可能是彼此正交甚至互斥的，单一共享表示无法同时满足。优化层面，各任务的损失尺度、收敛速度、噪声水平差异很大，损失大或收敛慢的任务会主导共享参数的更新。

关键在于，shared-bottom把共享程度硬编码成了"完全共享"，没有留任何调节余地。而任务相关性通常是未知的，甚至在训练过程中是变化的。这就是MMoE要解决的问题。

## 4.2 MMoE

Ma等人在KDD 2018的《Modeling Task Relationships in Multi-task Learning with Multi-gate Mixture-of-Experts》给出了一个极简的答案：底层换成一组共享专家，但每个任务拥有自己的门控。

$$ y_k = h_k\left( \sum_{i=1}^{n} g^k_i(x) \cdot f_i(x) \right), \qquad g^k(x) = \mathrm{softmax}(W_{gk} x) $$

其中 $k$ 索引任务，$h_k$ 是任务专属的塔，$f_i$ 是共享专家。任务A的门可以偏重专家1和2，任务B的门偏重专家2和3，重叠部分即为实际共享的表示。共享程度交给模型自己学，不用人来指定。

这个改动的性价比极高。门控只是一层从输入维度到专家数量的线性映射加softmax，参数量相对整个模型可以忽略，推理时增加的计算量也几乎不可测。相比之下它带来的灵活性是结构性的：当任务确实相关时，各门控会学到相似的分布，模型退化为近似shared-bottom。当任务不相关时，各门控会学到分离的分布，模型近似退化为若干独立模型。也就是说MMoE把shared-bottom和"完全不共享"两个极端都包含为特例，并允许连续插值。

论文的实验设计值得单独称赞，它在方法论上比结构创新更有借鉴价值。作者构造了一组合成数据，其中两个回归任务的目标由两个正弦变换生成，通过一个显式参数控制两个任务标签之间的皮尔逊相关系数。这样就能在相关系数从1降到接近0的连续区间上，比较shared-bottom、单门MoE（OMoE）与MMoE三者的表现。结果非常清晰：相关性高时三者接近，OMoE甚至略优于MMoE（因为参数更少）。相关性下降时shared-bottom迅速恶化，OMoE也开始退化，而MMoE的性能曲线最平缓。这个受控实验直接验证了"多门是用来对抗低任务相关性的"这一因果主张，而不只是展示了一个更好的基准分数。

论文还观察了门控权重的分布，发现在低相关设置下不同任务的门控确实学到了明显分离的专家偏好，这为结构解释提供了直接证据。

## 4.3 工业落地：YouTube 的多目标排序

Zhao等人在RecSys 2019的《Recommending What Video to Watch Next: A Multitask Ranking System》是MMoE最有影响力的工业落地案例，也是理解这条路线实际约束的最佳材料。

该系统需要同时优化两组目标：参与度目标（点击、观看时长等）和满意度目标（点赞、评分、不感兴趣反馈等）。这两组目标之间存在结构性张力。最大化观看时长的内容与用户事后评价高的内容并不一致，这正是典型的低相关甚至负相关任务场景。系统把多个目标按这两组划分，用MMoE承载共享，各目标有自己的门与塔，最终排序分数由各目标预测值按业务权重融合。

论文另一个被广泛引用的设计是位置偏差的处理。训练数据中的点击行为受到展示位置的强烈影响，直接学习会把位置效应混入内容相关性。做法是加一个只吃位置特征与设备特征的浅层塔，把它的输出与主塔输出相加后再过sigmoid，训练时让浅层塔吸收位置带来的偏差，线上推理时把这一项置零。这个技巧与MMoE本身无关，但它说明了工业多任务系统的真实复杂度：结构上的多任务只是其中一层，还需要同时处理选择偏差、反馈延迟、目标融合等问题。

从工程角度还有一点值得注意：该系统的专家数量是个位数到十几个的量级，且全部稠密计算。这与第三章动辄数百专家、只激活其中几个的做法形成鲜明对比，原因将在4.5节说明。

## 4.4 后续演进

**CGC与PLE。** Tang等人在RecSys 2020的《Progressive Layered Extraction》指出MMoE仍有未解决的问题，并给它起了一个形象的名字，跷跷板现象（seesaw phenomenon）：调整模型使某个任务提升时，另一个任务往往同步下降，很难找到多个任务同时优于单任务基线的配置。原因在于MMoE中所有专家仍然被所有任务共享，门控只是软化了冲突而没有消除它：一个专家的参数依然接收来自全部任务的梯度。

PLE的解法分两步。第一步是CGC（Customized Gate Control）：把专家显式分成任务专属组与共享组，任务k的门控只能选择"任务k的专属专家 + 共享专家"，看不到其他任务的专属专家。这样专属专家的参数只被单一任务的梯度更新，冲突被硬性隔离。第二步是把CGC结构堆叠多层，形成渐进抽取：底层做粗粒度的共享与分离，上层做更精细的组合，层间的共享专家还配有额外的门控来融合下层各组的输出。

PLE在腾讯的视频推荐场景上取得了多任务同时改善的结果，并获得了当年的最佳论文。它至今是工业多目标排序中的主流选择之一，实际使用频率可能高于原始MMoE。

**结构化共享的其他方案。** SNR（Sub-Network Routing，Ma等，AAAI 2019）把共享的粒度从"专家"下降到"子网络之间的连接"，用可学习的二值变量（配合软化技巧）决定子网络之间是否连通，从而把参数共享结构本身作为可搜索对象。这条路线与神经架构搜索有交集，灵活度更高但训练更复杂，工业落地不如PLE广泛。

**正交的一条思路：任务因果链。** 需要提到ESMM（Ma等，SIGIR 2018）作为对照。它处理的是转化率预估中的样本选择偏差与数据稀疏问题，做法是利用点击与转化之间的因果链条，在全空间上同时建模点击率与"点击且转化率"，把转化率作为两者之商隐式学习。它也是多任务结构，但共享方式来自任务之间的概率关系约束，而非可学习的门控。这提醒我们：多任务建模并不只有"柔性共享"一种范式，当任务之间存在已知的结构关系时，把关系编码进模型往往比让模型自己学更有效。

## 4.5 为什么这一支没有走向稀疏

这是理解两条路线差异的关键问题，答案有三层。

最直接的一条是专家数量本来就少。这条路线的专家数在个位数到十几个之间，稀疏化能省下的绝对计算量很有限，而路由带来的实现复杂度、训练不稳定和负载均衡开销是固定成本。收益成本比不划算。

再上一层是在线延迟约束。推荐系统的排序模型通常要在几十毫秒内对成百上千个候选打分。稀疏路由引入的动态形状、gather/scatter操作、以及可能的负载不均，都会让延迟的尾部分布变差。而工业系统关心的是p99延迟而非平均延迟。相比之下，稠密的小专家组可以编译成规整的批量矩阵乘，延迟可预测。

更硬的一条是训练稳定性的优先级。推荐模型通常以在线学习或高频重训的方式运行，模型每天甚至每小时更新。稀疏MoE的训练不稳定性在这种流水线里是不可接受的运维风险，没有人能在每天的例行重训里去处理损失尖峰。

还有一层，动机上根本不需要稀疏化。稀疏化的目的是在算力预算下装入更多参数，而推荐排序模型的瓶颈通常不在模型参数量（embedding表往往才是参数大头），而在特征工程、样本构造和多目标平衡。稀疏MoE解决的不是这条路线的痛点。

## 4.6 与损失层面方法的关系

MMoE一族是结构层面的解法：通过改变参数共享的拓扑来减少冲突。与之并行存在一条优化层面的路线，通过操纵梯度或损失权重来达到类似目的。

主要代表包括：不确定性加权（Kendall等，2018）用各任务的同方差不确定性自动确定损失权重。GradNorm（Chen等，2018）动态调整权重使各任务的梯度范数保持平衡。PCGrad（Yu等，2020）在梯度冲突时把一个任务的梯度投影到另一个梯度的法平面上，直接消除冲突分量。MGDA一族把多任务视为多目标优化，求帕累托方向。

两条路线是互补而非竞争的。结构方法解决"哪些参数被共享"，优化方法解决"共享参数上的更新如何合成"。工业实践中经常同时使用，比如用PLE做结构、再用不确定性加权或人工调参处理损失尺度。优化方法有额外开销。PCGrad一类需要保存各任务的独立梯度，显存开销随任务数线性增长，在任务数多时不实用。

# 五、两条分支的对比与交汇

## 5.1 系统性对比

|维度|稀疏MoE|MMoE / PLE|
|---|---|---|
|核心动机|参数容量与计算量脱钩|缓解多任务负迁移|
|要回答的问题|每个token该算多少|每个任务该共享多少|
|门控数量|1个路由器|每个任务1个|
|激活方式|top-k稀疏，多数专家不计算|全部专家稠密计算|
|专家数量|8到数百|4到十几个|
|单专家规模|一个完整前馈层或其切片|若干层MLP，通常较小|
|训练目标数|通常1个|多个，且可能相互冲突|
|主要失效模式|专家坍塌、路由抖动、token丢弃|跷跷板现象、门控趋同|
|主要成本|显存、all-to-all通信|计算量随专家数线性增长|
|评价标准|等算力下的损失 / 下游能力|各任务是否同时优于单任务基线|
|典型场景|LLM预训练与推理|推荐、广告、搜索的多目标排序|

从这张表可以看出，两者共享的只有"专家 + 门控"这个形式框架，其余几乎全部不同。特别值得强调的是最后一行评价标准的差异：稀疏MoE的成功标准是单一损失在给定算力下更低。MMoE的成功标准是多个指标的帕累托改进。这两种标准所需的实验设计、消融方式、甚至对"更好"的定义都不一样，这也是两条社区文献难以互相借鉴的深层原因。

## 5.2 概念收敛：共享专家的独立发明

前文提到过一个现象：两条路线各自独立地发明了"任务/路由专属专家 + 常驻共享专家"这一结构。PLE在2020年提出它，用来隔离任务间的梯度冲突。DeepSeekMoE在2024年提出它，用来减少路由专家之间的知识冗余。两者的实现细节甚至相当接近，都是把专家分组，一组无条件参与，另一组条件性参与。

这种收敛说明了什么？一个可能的解释是：任何基于条件计算的分工机制都面临同一个张力。完全分离会导致通用知识被重复学习，浪费容量。完全共享会导致特化知识被相互干扰，损害专精。而"一部分常驻 + 一部分条件"是这个张力最简单的显式解法。它之所以在两个动机完全不同的场景中都成立，是因为张力本身来自条件计算的结构，而非来自具体任务。

这个观察有实际价值：它提示两条路线之间存在尚未被充分利用的迁移空间。比如PLE的渐进分层抽取思想在稀疏MoE中几乎没有对应物（现有做法是各MoE层独立路由，层间没有显式的共享结构设计）。反过来，稀疏路线积累的负载均衡与路由稳定性技术，在多任务门控出现趋同退化时也许有借鉴价值。

## 5.3 命名谱系对照表

这一节专门用于消除缩写混乱。下表整理了容易混淆的名称：

|名称|全称|提出时间|条件变量|稀疏性|领域|
|---|---|---|---|---|---|
|MoE|Mixture of Experts|1991|输入|稠密（原始版）|通用|
|Sparse MoE|Sparsely-Gated MoE|2017|输入（token）|top-k稀疏|LLM|
|MMoE|Multi-gate MoE|2018|任务身份（多门）|稠密|推荐 / 多任务|
|PLE / CGC|Progressive Layered Extraction|2020|任务身份 + 硬分组|稠密|推荐 / 多任务|
|MoME|Mixture-of-Modality-Experts|2021|模态（硬路由）|按模态选择|视觉语言|
|LIMoE|Language-Image MoE|2022|输入token（学习路由）|稀疏|多模态对比学习|
|Soft MoE|Soft MoE|2023|输入（slot加权）|稠密|视觉|
|MMoE（多模态语境）|Multimodal MoE|2023年底起|模态交互|视具体工作|多模态融合|
|Task-MoE|Task-level MoE|2021起|任务 / 语言id|稀疏|多语言翻译|

阅读文献时的实用建议：看到MMoE先确认年份与发表场所。KDD、RecSys、CIKM一类场所的MMoE基本都指Multi-gate。CVPR、ACL、ICML上2024年之后出现的MMoE则可能指Multimodal。更可靠的判别方法是看门控数量。如果门控数等于任务数，是Multi-gate。如果门控数为一而专家按模态划分，是多模态变体。

## 5.4 多模态 MoE：第三条支流

多模态可以视为介于两条主线之间的第三条支流，它同时借用了两边的元素。

**模态专属 + 跨模态共享 + 模态感知路由。** 这是目前最主流的设计模式。以语音文本方向的MoST为代表，其模态感知MoE模块由三部分组成：模态专属的专家组、跨模态共享的专家、以及利用模态标识信息做路由的模态感知路由器。视觉语言方向的VLMo的MoME更早采用了类似思路，但路由是硬性的：视觉token走视觉专家，文本token走文本专家，图文对走融合专家，不需要学习。这个"硬路由"设计与MMoE的"学习门控"形成对照：当条件变量（模态）是已知且离散的时候，根本不需要学习路由。

**学习路由的多模态MoE。** LIMoE（Mustafa等，2022）走另一条路：不告诉模型哪个token属于哪个模态，让路由器自己学。这带来一个新问题。模态不平衡会导致路由偏向数据量大的模态，论文引入了基于熵的正则项来缓解。这条路线的吸引力在于它允许跨模态的自发共享，路由器可能发现某些视觉与文本token应该走同一专家。代价是训练更难控制。

**任务条件专家。** 这才是MMoE思路在多模态里的真正对应物：专家按任务而非按模态划分，每个专家服务一类多模态任务，用来缓解多任务目标之间的梯度冲突。医学诊断方向的M4oE把Soft MoE技术用于构建模态专属的专家池，再叠加一个任务专属的共享模态池来产生任务引导的特征，从而同时建模动态模态融合与模态-任务依赖。SAME则给出了另一种条件变量：路由放在时间维度上，智能体每进入一个新位置就依据当前融合的多模态表示重选专家，用状态而非任务身份来做分工。这一支相对小众，但它是"MMoE思想是否能进入多模态大模型"这个问题的正面答案。

**给已有模型加多模态能力的三条路径。** 当前实践中主要有三种做法：从零训练一个MoE架构的多模态模型，算力开销巨大，可扩展性受限。把已有稠密LLM在多模态微调阶段扩展为MoE，这条路便宜但由于专家间参数冗余高，专精程度往往不足，且受基座模型规模与能力限制。扩展一个已经预训练好的MoE基座LLM使其获得多模态能力，这条路既避免了从零训练的开销，也不受原模型语言能力上限的约束，且由于基座在大规模文本上预训练时专家已形成分化，后续多模态适配中出现冗余专精的可能性较低。第三条路径近来受到较多青睐。

**一个必要的澄清。** 做多模态并不必然需要MoE。稠密路线的视觉语言模型仍然是主流且效果优秀。MoE在多模态中是可选的容量扩展手段，附带能缓解模态间的相互干扰（比如加入视觉数据后语言能力退化的问题）。把"多模态"与"必须用MMoE"联系起来是一个常见但没有依据的推论。

## 5.5 选型指引

把前面的讨论压缩成一组判断规则：

- 单一训练目标，追求在固定算力下提升模型能力，显存充裕 → 稀疏MoE。
- 单一训练目标，显存紧张 → 稠密模型，或极高稀疏比的小MoE配合专家offload。
- 多个训练目标，任务相关性高 → 先试shared-bottom，简单且参数少。
- 多个训练目标，任务相关性未知或较低 → MMoE作为基线，PLE作为进一步选择。
- 多个训练目标且出现跷跷板 → PLE的硬分组，或考虑任务间是否存在可利用的因果结构（ESMM一类）。
- 多模态，条件变量是已知离散的模态 → 模态硬路由（MoME一类），不必学习门控。
- 多模态，希望自发发现跨模态共享 → 学习路由 + 模态平衡正则。
- 严苛在线延迟约束 → 避免稀疏路由，用稠密小专家组。

# 六、系统与工程实践

## 6.1 训练侧

MoE训练的核心特征是引入了专家并行（EP）这一新的切分维度，需要与已有的数据并行（DP）、张量并行（TP）、流水线并行（PP）组合使用。典型配置是专家沿EP维度分布，非专家参数走DP或TP，PP用于层间切分。

这里的主要难点是通信与计算的重叠。每个MoE层需要两次all-to-all，若串行执行会造成设备空转。常见优化包括：把token分块，让第一块的专家计算与第二块的通信重叠。把all-to-all与相邻层的注意力计算重叠。在拓扑层面限制每个token可跨越的节点数，把大部分通信约束在节点内的高带宽域中。DeepSeek系列公开的实践中还包括专家的动态重排（把高负载专家副本化并跨节点重分布），以在长期训练中维持均衡。

另一个方向是消除token丢弃。容量因子机制虽然让形状静态化从而便于编译，但代价是质量损失和一个需要调节的超参数。Megablocks一类工作把MoE层的计算重写为块稀疏矩阵乘，从而支持变长的专家负载，实现所谓dropless MoE。这需要专门的内核，但省去了容量因子调参，且在负载不均时不损失token。

训练框架层面，DeepSpeed-MoE、Tutel、FasterMoE等系统提供了不同侧重的实现：有的侧重通信优化，有的侧重动态并行策略切换，有的侧重推理阶段的专家剪裁。

## 6.2 推理侧

推理阶段MoE的行为与训练阶段有一个重要差异：批量大小对专家激活分布的影响。单条请求解码时，每步只有一个token，激活的专家数量等于k，此时绝大部分权重是闲置的。推理因此严重受显存带宽限制，计算单元利用率很低。但当批量增大时，不同请求的token会路由到不同专家，被激活的专家总数迅速上升，最终接近全部专家。也就是说MoE的稀疏性优势在大批量下会部分消失，因为每个专家都需要被加载，只是每个专家处理的token变多了。

这个特性带来几个实际推论。第一，MoE模型的吞吐-延迟曲线形状与稠密模型不同，需要单独做容量规划。第二，小批量场景（比如本地单用户推理）下MoE的收益主要来自算力而非带宽，因此在带宽受限的消费级硬件上收益有限，除非配合量化。第三，专家offload与预取成为一个有意义的方向：把不常用的专家放在CPU内存或磁盘上，根据路由结果按需加载，并利用相邻层路由的相关性做预测性预取。这条路能让超出显存的模型跑起来，代价是延迟。

MoE与投机解码的相互作用也值得注意。投机解码依赖一次前向验证多个token，这会改变每步激活的专家集合，可能抵消一部分投机带来的收益，具体影响取决于草稿token之间的路由相似度。

## 6.3 压缩侧

MoE的结构特性给压缩提供了新的抓手，也带来了新的陷阱。

专家剪枝与合并是最直接的思路：统计各专家的激活频率或贡献度，去掉低价值专家，或把相似专家合并。难点在于重要性度量。激活频率高的专家不一定重要，可能只是在处理高频低信息token。

专家级混合精度量化近来成为主流范式：利用MoE的稀疏激活与专家贡献不均等结构性质，给更关键的专家分配更高位宽，对次要专家做更激进的压缩，重要性通常以校准集上的专家选择频率作为代理。这个范式在纯文本MoE上工作良好，但迁移到多模态MoE时会出问题。把所有token的激活统计不分模态地聚合起来，会使专家重要性估计失真，导致明显的性能退化。近期工作因此转向按模态分解的重要性度量，或引入模态亲和度感知的目标函数。这是一个典型的例子：MoE的结构特性使得几乎每一类通用优化技术都需要被重新审视一遍。

## 6.4 多模态特有的效率问题

多模态MoE在部署上有两个额外的困难，都源于视觉token的特性。

一是信息异质性：视觉token数量庞大且冗余度高，其中很多与语义关键的文本token在计算上被同等对待，造成资源浪费。二是模态动态性：不同任务的视觉-文本token比例差异很大（纯文本对话、单图问答、多图或视频理解的比例可以相差一两个数量级），导致按固定假设分配的资源出现错配。

这两点使得基于token计数的传统负载均衡方法在多模态场景下失效，专家并行推理时的长尾效应（straggler effect）被显著放大。应对方案包括模态感知的容量缩放（按模态区分地给专家分配容量预算，可以做成免训练的推理框架）、视觉token的预先剪枝或合并、以及按模态分离的路由统计与调度。

## 6.5 生态现状

截至2026年中，MoE的支持已经从"需要专门框架"变成了主流推理引擎的标准功能。vLLM、SGLang一类引擎对主流MoE模型提供了开箱支持，包括专家并行、FP8权重、以及针对MoE的分块调度。训练侧的Megatron、DeepSpeed等框架也都内置了MoE层与相应的并行策略。

对使用者而言，不同引擎对同一模型的MoE实现细节可能不同（比如是否dropless、路由器精度、量化粒度），这会导致输出存在细微差异。在做严格的模型对比实验时，引擎与配置需要作为变量记录。

# 七、开放问题与展望

## 7.1 专家真的"专精"了吗

这是最根本也最尴尬的问题。"专家"这个命名暗示了语义分工，但证据是混杂的。

支持的一侧：ST-MoE等工作在编码器中观察到了可解释的专精模式，某些专家明显偏好标点符号、数字、专有名词或特定语言。DeepSeekMoE的细粒度切分与共享专家隔离设计，在消融实验中确实显示出减少冗余的效果，间接支持专精假说。

反对的一侧：Mixtral的技术报告中对路由分布的分析并未发现明显的按主题分工，专家选择更多与token的句法位置相关而非语义领域。更有力的反证是Hash Layers。用固定哈希做完全随机的分配，效果与学习路由相差不大。如果语义分工是收益的主要来源，这个结果不该出现。

一个折中的解释是：MoE的主要收益可能来自参数量的纯粹增加与梯度稀疏化带来的隐式正则，而不是可解释的语义分工。路由只需要"稳定且均衡"，不需要"有意义"。如果这个解释成立，那么"专家"这个隐喻在很大程度上是误导性的，而路由研究的方向应该从"如何学到更好的分工"转向"如何更稳定地维持一个任意但一致的划分"。这个问题目前没有定论，但它对研究议程的影响很大。

## 7.2 路由机制的理论空白

几个基础问题缺乏理论刻画。为什么top-k这种不可微的离散选择能通过梯度下降训练起来（只有被选中专家的门控权重接收梯度，未选中专家几乎得不到信号），而实践中却不至于陷入初始划分？稀疏度是否存在理论上限：给定总参数N，激活参数A低到什么程度模型能力会崩塌，这个阈值与任务复杂度的关系是什么？专家粒度的最优值是否有解析形式？

细粒度MoE的scaling law工作在实证层面推进了第三个问题，但整体上稀疏路线的理论基础远落后于工程实践。这与深度学习的普遍状况一致，但在MoE上尤为明显，因为这里的设计空间（专家数、粒度、k、容量因子、放置、均衡强度）维度太高，缺乏理论指导时只能靠昂贵的实验搜索。

## 7.3 与长上下文架构的耦合

MoE优化前馈层，注意力改造优化序列维度，两者组合已成惯例。但组合方式基本还是简单叠加，缺少协同设计。一个开放的方向是：能否让路由感知上下文位置或注意力模式？比如让处理长程依赖的token走特定专家，或让路由决策复用注意力已经计算出的相似度信息。目前这类设计极少见，但从计算复用的角度看是有潜力的。

反向的问题同样存在：MoE层的存在会不会影响长上下文的信息传递？由于相邻位置的token可能被路由到不同专家，经过不同的变换，理论上会引入表示空间的不连续性。这个效应在长序列上是否累积，缺乏系统研究。

## 7.4 任务条件与模态条件能否统一

从5.4节可以看出，条件计算的"条件"可以是token内容、任务身份、模态类型，甚至语言id。目前这些变体各自独立实现，没有统一框架。一个自然的问题是：能否设计一个路由器，同时接受多种条件信号并学会它们的相对重要性？

这在多任务多模态的大模型中会变得实际起来。一个需要同时处理图文视频、并优化多个训练目标的模型，理论上需要同时做模态分工与任务分工。目前的做法是分层处理（底层按模态、上层按任务），但这是一个人为的设计选择，未必最优。统一的条件路由框架若能建立，会是两条谱系真正的合流点。

## 7.5 从架构选择到系统对象

最后一个观察是趋势性的。2021年前后，MoE还是一个可选的架构技巧。到2026年，它已经演变成一个需要专门优化的系统对象，围绕它形成了专门的并行策略、专门的量化方法、专门的负载均衡研究、专门的推理调度算法，甚至专门的硬件讨论（专家权重的存储层级设计）。

这个变化意味着MoE的进一步演进将越来越受系统约束而非建模灵感驱动。未来的结构改动大概率会以"是否易于并行、是否易于量化、是否延迟可预测"为首要筛选条件。这既是成熟的标志，也可能是创新空间收窄的信号。多任务路线在推荐系统领域早已经历过同样的过程。PLE之后结构创新明显放缓，重心转向了特征、样本与目标融合。

# 八、结论

回到开篇的论点。MoE与MMoE的关系，是同一个1991年的想法被两个不相干的动机各自征用的结果，跟"通用版与专用版"没有关系。

稀疏MoE是关于"算多少"的技术。它把专家当作容量单元，通过条件激活让参数量摆脱算力的束缚，服务于单一训练目标下的规模扩张。它的成败取决于路由是否稳定、负载是否均衡、通信是否可控，而这些都是系统问题。到2026年，它已经成为大模型的默认架构，几乎所有重要的开源权重发布都采用稀疏MoE，稀疏比推进到二三十倍甚至更高。

MMoE是关于"共享多少"的技术。它把专家当作共享单元，通过为每个任务配备独立门控让参数共享的程度成为可学习量，服务于多个可能冲突的训练目标。它的成败取决于任务之间的冲突是否被有效隔离，而这是建模问题。它至今保持稠密，主要活跃在推荐、广告与搜索的多目标排序中，PLE一类的硬分组变体是当前的实际主力。

两者的相似只在机制层面。若要在介绍中只留一句话，我会选：MoE让不同的token走不同的路，MMoE让不同的任务走不同的路。路是同一批，走法的含义完全不同。

而两条路线各自独立发明出"共享专家 + 专属专家"这一结构，或许提示了条件计算领域一个更普遍的规律：任何试图在容量与专精之间取得平衡的机制，最终都会需要一部分无条件参与的公共部分。这个规律的适用边界，以及任务条件与模态条件能否统一进同一个路由框架，是这个领域接下来值得关注的问题。

# 附录 A：常见误解速查

以下八条是笔者在技术讨论中反复遇到的误解，附上简短的纠正，可作为全文的速查摘要。

**误解一：MoE是为多任务学习设计的。** 不是。MoE解决的是条件计算问题，即让参数容量摆脱算力约束。多任务只是MMoE那一支的目标。现代大模型里的MoE在训练时只有一个目标函数，整个过程与多任务无关。

**误解二：MMoE是MoE的升级版，前者包含后者。** 两者是同一祖先的两个分支，没有继承关系。MMoE沿用了稠密的原始形态，只改动了门控的数量。稀疏MoE沿用了单一门控，只改动了激活方式。二者在此后的十年里几乎没有互相引用。

**误解三：Transformer里的MoE就是MMoE。** 不是。Transformer中的是单路由器、top-k稀疏激活。MMoE的标志特征是门控数等于任务数且全部专家稠密计算，这在语言模型里不存在。MMoE确实会与Transformer同时出现，但位置是"Transformer编码序列 + MMoE作为上层多任务头"，属于拼接而非嵌入。

**误解四：做多模态需要用MMoE。** 不需要。稠密的视觉语言模型仍是主流。多模态若使用专家机制，用的是模态感知路由或模态专属专家，条件变量是模态而非任务。看到多模态论文里的MMoE，先确认它指的是Multimodal还是Multi-gate。

**误解五：MoE能省显存。** 恰恰相反。稀疏MoE省的是每token的计算量，全部专家权重仍须常驻。总参数决定显存下限，激活参数决定算力成本，这是两笔需要分开核算的预算。自托管的真正门槛通常在前者。

**误解六：专家学会了按主题分工。** 证据混杂。部分工作观察到句法层面的专精（标点、数字、特定语言），但按语义主题的分工证据薄弱，而固定哈希路由能取得接近的效果这一事实构成了有力反证。收益可能更多来自参数量增加与梯度稀疏化，而非可解释的分工。

**误解七：稀疏度越高越划算。** 存在上限。激活参数过低时，模型在多步推理一类任务上的能力会明显退化，即使总参数很大。稀疏比的合理区间还与专家粒度耦合，需要联合调整而非单独推高。

**误解八：MMoE也应该稀疏化以提升效率。** 在推荐排序场景下不划算。专家数本就只有十几个，能省的绝对计算量有限，而稀疏路由带来的延迟尾部恶化与训练不稳定在高频重训、几十毫秒延迟预算的在线系统里是难以接受的成本。

# 参考文献

## 起源与理论基础

1. Jacobs, R. A., Jordan, M. I., Nowlan, S. J., & Hinton, G. E. (1991). Adaptive Mixtures of Local Experts. _Neural Computation_, 3(1).
2. Jordan, M. I., & Jacobs, R. A. (1994). Hierarchical Mixtures of Experts and the EM Algorithm. _Neural Computation_, 6(2).
3. Clark, A., et al. (2022). Unified Scaling Laws for Routed Language Models. _ICML_.
4. Krajewski, J., et al. (2024). Scaling Laws for Fine-Grained Mixture of Experts. _arXiv_.

## 稀疏路线

5. Shazeer, N., et al. (2017). Outrageously Large Neural Networks: The Sparsely-Gated Mixture-of-Experts Layer. _ICLR_.
6. Lepikhin, D., et al. (2020). GShard: Scaling Giant Models with Conditional Computation and Automatic Sharding. _arXiv / ICLR 2021_.
7. Fedus, W., Zoph, B., & Shazeer, N. (2021). Switch Transformers: Scaling to Trillion Parameter Models with Simple and Efficient Sparsity. _JMLR_.
8. Du, N., et al. (2022). GLaM: Efficient Scaling of Language Models with Mixture-of-Experts. _ICML_.
9. Zoph, B., et al. (2022). ST-MoE: Designing Stable and Transferable Sparse Expert Models. _arXiv_.
10. Lewis, M., et al. (2021). BASE Layers: Simplifying Training of Large, Sparse Models. _ICML_.
11. Roller, S., et al. (2021). Hash Layers For Large Sparse Models. _NeurIPS_.
12. Zhou, Y., et al. (2022). Mixture-of-Experts with Expert Choice Routing. _NeurIPS_.
13. Puigcerver, J., et al. (2023). From Sparse to Soft Mixtures of Experts. _ICLR 2024_.
14. Komatsuzaki, A., et al. (2022). Sparse Upcycling: Training Mixture-of-Experts from Dense Checkpoints. _ICLR 2023_.
15. Jiang, A. Q., et al. (2024). Mixtral of Experts. _arXiv_.
16. Dai, D., et al. (2024). DeepSeekMoE: Towards Ultimate Expert Specialization in Mixture-of-Experts Language Models. _ACL_.
17. DeepSeek-AI. (2024). DeepSeek-V3 Technical Report. _arXiv_.
18. Qwen Team. (2024). Qwen1.5-MoE: Matching 7B Model Performance with 1/3 Activated Parameters.

## 多任务路线

19. Ma, J., Zhao, Z., Yi, X., Chen, J., Hong, L., & Chi, E. H. (2018). Modeling Task Relationships in Multi-task Learning with Multi-gate Mixture-of-Experts. _KDD_.
20. Zhao, Z., et al. (2019). Recommending What Video to Watch Next: A Multitask Ranking System. _RecSys_.
21. Tang, H., Liu, J., Zhao, M., & Gong, X. (2020). Progressive Layered Extraction (PLE): A Novel Multi-Task Learning (MTL) Model for Personalized Recommendations. _RecSys_（最佳论文）.
22. Ma, J., et al. (2019). SNR: Sub-Network Routing for Flexible Parameter Sharing in Multi-Task Learning. _AAAI_.
23. Ma, X., et al. (2018). Entire Space Multi-Task Model: An Effective Approach for Estimating Post-Click Conversion Rate. _SIGIR_.
24. Kendall, A., Gal, Y., & Cipolla, R. (2018). Multi-Task Learning Using Uncertainty to Weigh Losses. _CVPR_.
25. Chen, Z., et al. (2018). GradNorm: Gradient Normalization for Adaptive Loss Balancing. _ICML_.
26. Yu, T., et al. (2020). Gradient Surgery for Multi-Task Learning (PCGrad). _NeurIPS_.

## 多模态

27. Bao, H., et al. (2021). VLMo: Unified Vision-Language Pre-Training with Mixture-of-Modality-Experts. _NeurIPS 2022_.
28. Mustafa, B., et al. (2022). Multimodal Contrastive Learning with LIMoE: the Language-Image Mixture of Experts. _NeurIPS_.
29. Lin, B., et al. (2024). MoE-LLaVA: Mixture of Experts for Large Vision-Language Models. _arXiv_.
30. Li, Y., et al. (2024). Uni-MoE: Scaling Unified Multimodal LLMs with Mixture of Experts. _arXiv_.
31. Jiang, & Shen. (2024). M4oE: 多模态多任务专家池设计. _arXiv_.
32. Zhou, G., et al. (2024). SAME: Learning Generic Language-Guided Visual Navigation with State-Adaptive Mixture of Experts. _arXiv:2412.05552 / ICCV 2025_.
33. Lou, Y., et al. (2026). MoST: Mixing Speech and Text with Modality-Aware Mixture of Experts. _arXiv:2601.10272_.
34. SMAR: Soft Modality-Aware Routing Strategy for MoE-based Multimodal Large Language Models. _arXiv 2506.06406_.
35. Tackling Multimodal Learning Challenges with Mixture-of-Expert: A Survey. _arXiv 2605.27431_.

## 系统与压缩

36. Rajbhandari, S., et al. (2022). DeepSpeed-MoE: Advancing Mixture-of-Experts Inference and Training. _ICML_.
37. Hwang, C., et al. (2022). Tutel: Adaptive Mixture-of-Experts at Scale. _MLSys 2023_.
38. Gale, T., et al. (2022). MegaBlocks: Efficient Sparse Training with Mixture-of-Experts. _MLSys 2023_.
39. MACS: Modality-Aware Capacity Scaling for Efficient Multimodal MoE Inference. _arXiv 2605.05225_.
40. MODE: Modality-Decomposed Expert-Level Mixed-Precision Quantization for MoE Multimodal LLMs. _arXiv 2606.17118_.

> 说明：文中2026年的模型规模数据取自各模型公开发布材料与同期综述整理，不同来源口径偶有差异。多模态与系统优化部分的部分条目为2026年上半年的近期工作，编号31–35、39–40的作者与出处信息请在引用前按arXiv原文核对。