# 一、教科书给了流水线，没给一张表

主成分分析（PCA）和Transformer，在方法论上算不算同一类东西？

这个问题不好答。PCA对协方差矩阵做一次特征分解，参数当场就出来，没有迭代、没有学习率、没有loss曲线。k近邻连参数都没有，训练集本身就是模型。高斯过程压根不给确定的参数值，给的是一个后验分布，用的时候做积分。

这三样今天都还在大规模使用，却在最核心的那一步上跟Transformer走的是完全不同的路。要回答它们算不算同类，得先有一张能同时装下它们的表。

## 1.1 之前别人是怎么写的

这件事有人做过，大致三种写法。

最老的一种是判定式的。Tom Mitchell在1997年的教科书里给了那个被引用了无数次的定义：一个程序在任务T上的表现，用度量P衡量，随着经验E增加而提高，就说它在学习。T、P、E三个字母干净利落，下棋的例子也好懂，任务是下国际跳棋，度量是胜率，经验是自己跟自己对弈。

T、P、E的用途是划界。它回答"这算不算机器学习"，把学习系统跟普通程序分开。它不告诉你做一个系统要经过哪些环节，也不区分梯度下降和特征分解，PCA和Transformer在这个定义下长得完全一样。

工业界用得最久的是流程式的。1999年发布的CRISP-DM把数据挖掘切成六个阶段：业务理解、数据理解、数据准备、建模、评估、部署。这套东西活了二十多年，项目经理拿它排期没有问题，跟客户对齐范围也方便。

它的缺陷是"建模"那一格是个黑箱。六个阶段里有五个在讲数据和流程，所有技术选择全压在剩下那一个词里。CRISP-DM从来没打算解决这个问题，它是项目管理工具，拿它去理解算法结构属于用错了地方。

跟本文最接近的是第三种，结构式的。Pedro Domingos在2012年那篇《A Few Useful Things to Know about Machine Learning》里提出，任何一个学习算法都可以拆成三个部件：

| 部件 | 它管什么 | 举例 |
|---|---|---|
| 表示（representation） | 模型能表达哪些函数 | 线性模型、决策树、神经网络 |
| 评价（evaluation） | 拿什么标准区分好坏 | 平方误差、交叉熵、似然 |
| 优化（optimization） | 怎么找出评价最高的那一个 | 梯度下降、凸规划、贪心搜索 |

这三个部件合起来，Domingos叫它三元组。它的用法是：从三列里各挑一个，拼出一个学习算法。线性模型加平方误差加梯度下降是一种，决策树加信息增益加贪心搜索是另一种。

三元组比前两种写法都锋利。它不描述项目流程，它描述算法的内部结构，而且明确说了三列可以自由组合。这个组合视角是对的，本文那张节点表就是它的加长版。

还有一个角度值得提一句。Leo Breiman在2001年那篇《Statistical Modeling: The Two Cultures》里，把建模界分成两种文化：一种假设数据由某个给定的随机模型生成，关心变量之间的关系。另一种把数据生成机制当成未知，直接用算法追预测精度。这篇文章解释了机器学习和统计学为什么长期互相看不上，但它给的是一条文化分界线，不是一张操作清单。

## 1.2 三元组的第三格取错了名字

三元组的问题出在第三列，那一列叫优化。

这个命名在2012年是合理的。当时深度学习刚起步，AlexNet那年才拿下ImageNet，主流还是凸优化和统计学习，支持向量机和逻辑回归确实都在做优化。

但这个名字把一个问题偷换成了某一类答案。第三列真正在问的是"模型是怎么被定下来的"，而"优化"只是众多答法里的一种。拿几个常用方法去填就知道了。

PCA不优化。它对协方差矩阵做一次特征分解，取前k个特征向量，确定性的一步到位。你可以事后证明这个解在重构误差意义下最优，但求解过程里没有任何搜索发生。

k近邻不搜索也不优化。Cover和Hart在1967年那篇论文里给出的规则，是把训练集原样存下来，来一个新点就找最近的邻居投票。这里既没有参数也没有目标函数。

高斯过程做的是积分。它对参数的后验分布求期望，从头到尾没有"找到最好的那一个"这回事。核函数的超参数确实常用边际似然去优化，但那是外层的一个小问题，模型本身的预测靠的是积分。

梯度提升树也不合。Jerome Friedman在2001年那篇《Greedy Function Approximation》里讲得很清楚，梯度提升是逐轮往模型上加一棵新树，每棵树用贪心分裂长出来。跑到第五百轮和跑到第一百轮，参数量差五倍。你没法说"优化"了一个固定的参数向量，因为参数向量根本不是固定长度的。

Hopfield网络最干脆。John Hopfield在1982年那篇发在PNAS上的论文里，权重是用赫布规则一次写进去的，$w_{ij}=x_ix_j$，看一眼要记住的模式就完事，没有任何迭代。这个方法在2024年拿了诺贝尔物理学奖，第七章还会回来说它。

生成对抗网络是反方向的例外。Ian Goodfellow等人在2014年提出它的时候，明确说这是一个两人博弈，要找的是纳什均衡。生成器和判别器各有各的目标函数，两个目标是对抗的，整个系统没有一个单独的量在被极小化。GAN训练不稳定的老毛病，根子就在这里，均衡点跟极小点的数学性质完全不同。

六个方法，全部卡在同一格上。这说明那一格的名字取错了，取成了一类具体答案的名字。

## 1.3 我这张表跟前面三种差在哪

四种写法放在一起对照，差别集中在两处：这张表回答什么问题，以及它怎么处理"模型是怎么定下来的"那一格。

| | 回答什么 | 那一格怎么写 | 装得下PCA吗 |
|---|---|---|---|
| Mitchell T/P/E | 这算不算学习 | 不涉及 | 装得下，但不区分 |
| CRISP-DM | 项目怎么排期 | 压缩成"建模"一个词 | 装得下，但不区分 |
| Domingos三元组 | 算法由哪些部件组成 | 命名为"优化" | 装不下 |
| 本文节点表 | 做一个系统要回答哪些问题 | 命名为一个问句，七个取值 | 装得下，填"特征分解" |

最后一列是全部差别的浓缩。前两种装得下是因为它们粒度太粗，粗到看不出区别。三元组粒度够细，但第三列填错了名字，于是PCA变成例外。

具体的改动有三处。

**那一格改成问句**。它问的是"怎么把参数定下来"，答案有七个：闭式解、特征分解、迭代优化、做积分、一次写入、随求解生长、离散搜索。梯度下降只是其中之一，恰好是过去十几年用得最多的那个。改一个词，六个例外一起消失，这种事通常说明改对了。

**构造块多一个节点**。它问的是你交付的是模型本身，还是一个包着模型的算法。温度采样、思维链、best-of-n、蒙特卡洛树搜索、检索增强都落在这一格里，2.6节展开。

**两个取值是新加的**。"离散搜索"和"随数据生长"，都是拿表去撞反例撞出来的，第五章会把撞的过程写出来。

其余部分是继承来的。组合视角来自Domingos，四大块的切法接近CRISP-DM的六阶段，本文做的是把部件从三个加到十八个，并且把重心从数据准备挪到求解。

判一张这样的表合不合格，我用三条标准。

**能填满**。拿去撞任意一个机器学习方法，每个节点都能填上一个值，或者能明确说出这一格为什么是空的。空格本身可以是信息，填不上又说不清才是失败。

**撞不动时能看出缺哪格**。一张好表被反例撞到时，反例会告诉你少了哪一格，而你不会想去质疑那个反例。这条比第一条重要。

**每格都真的在做区分**。如果所有模型在某一格上填的值都一样，这一格通常该合并掉。第四章那个"有没有外部真值"是唯一的例外，理由在第六章。

这张表不预测好坏。它不会告诉你注意力比循环好，也不会告诉你该用哪个方法。它只保证你在做选择的时候，知道自己一共要做多少个选择。

下面是全表。十八个节点，分四块。第二到第四章逐块细说。

| 块&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; | 要回答的问题                                | 节点数 |
| ---- | ------------------------------------- | --- |
| 一&nbsp;定题 | 要什么、化成什么可测量、拿什么数据、目标与数据谁先             | 4   |
| 二&nbsp;构造 | 候选函数是哪一堆、塞什么先验、参数化是什么形态、通路还是分布、交付的是什么 | 5   |
| 三&nbsp;求解 | 判据是什么、怎么把参数定下来、有没有内环、何时停、规模怎么分        | 5   |
| 四&nbsp;检验 | 拿什么比、有没有外部真值、上线后如何、懂不懂自己造了什么          | 4   |

先约定几个符号，后面会反复用。$W$是你真正想要的东西，$M$是把它变成的可测量指标，$L$是实际拿去算的判据。$\theta$是参数，$f_\theta$是参数化的函数，$\hat P_n$是你手上的数据集，$P$是那个你永远看不到的真实分布，$\varepsilon$是模型在$P$上的真实风险。

这几个字母的关系是全文的骨架：优化$L$，评价$M$，想要$W$，而这三样从来不是同一件东西。

# 二、定题与构造：前九个节点

## 2.1 三层代理：想要的、能测的、能算的

一个推荐系统的产品经理想要的是"用户觉得这个产品好用"。这是$W$，它没法被写成一个数。

于是它被换成点击率，或者停留时长，或者次日留存。这是$M$，能测量，能做A/B测试，能写进季度目标。然后工程师再把$M$换成一个可微的损失函数，比如交叉熵，这是$L$，能求梯度。

三层代理，每一层都在丢东西。梯度下降优化的是最里面那层$L$，季度汇报看的是中间那层$M$，公司真正在意的是最外面那层$W$，而这三层的最优点很少重合。点击率涨了三个点，用户可能只是被标题党骗进去然后更烦了。

这不是抽象的担忧。英国经济学家Charles Goodhart在1975年就说过，任何被观察到的统计规律，一旦被拿来当作调控目标，就会失效。他当时说的是货币政策，英格兰银行发现某些货币供应量指标跟通胀关系稳定，一开始按这些指标调控，关系就断了。人类学家Marilyn Strathern在1997年把它改写成了那个流传更广的版本：当一个度量变成目标，它就不再是一个好的度量。

在机器学习里这条定律的现身方式很具体。约翰霍普金斯大学的研究者在2024年的NAACL会议上报告，MMLU这个被广泛使用的评测基准里，约29.1%的测试题存在污染迹象。把污染的题目换成语义相同的干净版本重测，Mistral在GSM8K上的成绩最多掉了13个百分点。

分数是$M$，能力是$W$。当整个行业开始按$M$排名，$M$和$W$的关联就开始断裂。这里面没有谁在作弊，把公开的互联网文本全爬下来做训练数据，本来就会顺手把基准测试题爬进去。

还有一层更根本的东西没法测。$\varepsilon$，模型在真实分布$P$上的风险，这才是统计学习理论真正关心的量。它不可计算，因为你没有$P$，只有$\hat P_n$。所有的留出集、交叉验证、benchmark，都是在用$\hat P_n$的一部分去估计一个原则上估不出来的量。

这四层的关系值得记住：$\varepsilon$想知道但算不出，$W$想要但测不了，$M$能测但会被刷，$L$能算所以被算。整个行业每天在优化的，是这条链最里面那一环。

定题块的头三个节点分别对应这三层。第一个问"想要什么"，第二个问"化成什么可测量"，第三个问"拿什么数据"。三个都很基础，也都没有理论指导，全靠判断。

## 2.2 目标与数据谁先

前三个节点看起来是一条自然的顺序：先想清楚要什么，再决定怎么衡量，最后去收集数据。第四个节点问的是这条顺序本身：**目标和数据，哪个在前**。

监督学习是目标在前。你先决定要做猫狗分类，然后去收集带标签的猫狗照片。数据是为目标服务的，标注成本直接由目标决定，数据集的边界跟任务的边界重合。ImageNet就是这么造出来的，先定一千个类别，再去填图片。

基础模型把这个顺序整个倒过来了。实际流程是先有数据，互联网上能爬到的全部文本，然后为这堆数据造一个代理任务，预测下一个token，最后目标才由下游用户决定，而且是在模型训练完之后才决定的。GPT-3发布的时候，OpenAI并不知道两年后有人会拿它写代码。

这两条路在方法论上是两种完全不同的动作，所以必须是独立节点。把它塞进"拿什么数据"里面，就等于假设目标一定在数据之前，而这个假设在过去六年里已经被推翻了。

代理任务这个概念也值得单说。预测下一个token本身没有任何商业价值，没有人需要一台机器来续写句子。它的价值全在于：要把这件事做好，模型被迫学会语法、事实、推理链条。用一个没人要的任务去逼出一堆有人要的能力，这是自监督学习的全部机巧所在。

这一块的决定是全表里最不可逆的。$\hat P_n$一旦冻住，后面所有的计算都是在这个数据集上做的，模型能力的上界在这一步就定死了。你可以换架构、换优化器、加参数，但数据里没有的东西，不会从优化过程里长出来。

而这一块恰恰最缺理论指导。数据配比多少代码多少自然语言、去重去到什么强度、多语言比例怎么定、合成数据掺多少，这些问题今天基本靠试。发布的技术报告里这一部分往往写得最含糊，一半是商业机密，另一半是因为确实说不出所以然。

顺带说一句表征的归属。分词器（tokenizer）看起来像是构造决定，因为它明显是一种设计选择。但数据集实际存在的形式是一串token编号，不是文本，分词器一定，$\hat P_n$的物理形态就定了。

数字被切成什么样直接决定模型能不能做算术。所以它归定题，归在"拿什么数据"这一格里。

## 2.3 候选函数是哪一堆：函数族与归纳偏置

学习这件事，说到底是从一大堆候选函数里挑出一个。构造这一块的头两个节点，管的就是那一堆候选长什么样。

第一个节点问**候选函数是哪一堆**，术语叫函数族。线性函数、核方法撑起的再生核希尔伯特空间、分段常数（决策树）、卷积堆叠、循环结构、注意力堆叠、能量函数、无参数方法，这是一份还在变长的清单。

这一堆候选的范围有个正式名字，叫假设空间。它决定了你的模型原则上能表达哪些函数，也决定了哪些函数这个模型这辈子都够不着。

决策树能表达任意分段常数函数，但表达一条斜线要用无穷多段。线性模型表达斜线毫不费力，表达异或关系却做不到。选函数族就是选你愿意在哪些问题上吃亏。

第二个节点问**塞什么归纳偏置**，也就是在挑之前，先按什么规矩把那一堆候选缩小。卷积网络塞的是平移等变和局域性，图片里的猫平移一下还是猫。图神经网络塞的是置换等变，节点编号换一遍图不变。因果语言模型塞的是时间方向上的因果掩码，第n个词只能看见前面的词。

AlphaFold塞的是三维空间的旋转平移等变性，蛋白质转个方向还是同一个蛋白质。

这一格为什么必须存在，Wolpert在1996年那篇《The Lack of A Priori Distinctions Between Learning Algorithms》里已经证明过。在所有可能的目标函数上平均，任意两个学习算法的表现完全相同，哪怕其中一个是随机猜。这就是无免费午餐定理。

结论听起来很消极，实际含义很有建设性：算法之所以有用，全靠它对世界做了正确的假设。归纳偏置是唯一的信息来源，不往里塞先验，就什么也学不到。所谓"通用学习器"在数学上不存在。

这跟Rich Sutton在2019年那篇《The Bitter Lesson》看起来矛盾。他的观点是，七十年AI史反复证明，靠堆算力和通用方法（搜索与学习）的路线最终总能赢过精心编码的人类知识，而研究者总是很慢才接受这一点。

两件事其实都成立。Transformer的注意力机制本身就是一个归纳偏置，它假设序列里任意两个位置可以直接交互，而且这种交互的强度由内容决定。这个假设足够弱、足够通用，弱到能被大规模数据填满，同时又强到把"任意两个位置可以直接交互"这件事写死在了架构里。争的是偏置该有多强，从来不是要不要有。

## 2.4 参数化的三种形态

第三个构造节点问的是：**参数化是什么形态**。这一格容易被跳过，因为对神经网络来说答案太显然了。

最常见的是**固定**。神经网络在训练开始前参数量就定死了，七十亿就是七十亿，训练过程中一个都不多一个都不少。所有关于"模型大小"的讨论都默认了这个前提，模型大小是个可以印在发布会PPT上的数字。

第二种是**随求解生长**。梯度提升树每一轮加一棵新树，跑到第五百轮和跑到第一百轮，模型的参数量差了五倍。这里"模型多大"这个问题没有事先的答案，它是求解过程的产物。神经架构搜索也在这一类里，搜索过程本身在决定网络长什么样。

第三种是**无参数**。k近邻把训练集当模型，Cover和Hart在1967年那篇论文里给出的那条规则，本身就没有需要拟合的参数。核密度估计也是这一类。这类方法的存储开销随数据线性增长，推理成本也随之增长，这是它们在大数据时代退居二线的直接原因。

这一格跟求解块的"怎么把参数定下来"是联动的。填"随求解生长"的方法，在求解那一格上几乎必然也填"生长"，因为决定参数量和决定参数值是同一个过程。填"无参数"的，在求解那一格上填"不求解"。

我原来以为三个值够了。写到贝叶斯非参数方法的时候发现不够，这一格漏了一个值。第五章会补上，那是被反例撞出来的第一个坑。

## 2.5 一条通路，还是一个分布

第四个构造节点是很多人不会主动想到的一格：交付出来的是一条**计算通路**，还是一个**概率分布**。

通路的意思是，输入进去，沿着$f_\theta$算一遍，输出就出来了。逻辑回归、卷积网络、Transformer都是这样，前向传播一次，答案落地。计算量事先可以精确算出来，多少次浮点运算写在纸上。

分布的意思是，你定义的是一个概率密度，通常长成这个形状：

$$p(x)=\frac{1}{Z}e^{-E(x)/T}$$

$E$是能量函数，$T$是温度。这个式子直接照搬统计物理里的玻尔兹曼分布，能量越低的状态概率越大，温度越高分布越平。玻尔兹曼机、Hopfield网络、各类能量模型都是这一支。

麻烦全在分母那个$Z$上。$Z=\sum_x e^{-E(x)/T}$叫配分函数，它要对所有可能的状态求和。对一个有一百个二值单元的网络，状态数是$2^{100}$，这个和永远算不出来。

算不出$Z$的直接后果是：你有一个概率模型，却没法直接算出任意一个状态的概率，也没法直接采样。唯一的办法是跑马尔可夫链蒙特卡洛，从一个随机状态出发，按一定规则不断跳，让链子走够久走到平衡，那时的样本才算是从$p$里抽出来的。

这一格的取值会直接影响到求解那一块。选了分布，就多出一个采样时间$\tau$，而且这个$\tau$很可能落在训练的内循环里。第三章会展开，第七章会论证这一格是全表里最能预测成败的地方之一。

要说清楚的是，通路和分布不是"确定性对随机性"的区别。扩散模型是分布，但它在这一格上填分布的理由是它显式定义了一个概率密度，跟它采样时带不带随机数无关。判断标准是：你的模型对象是一个函数，还是一个密度。

## 2.6 交付的是网络，还是包着网络的算法

第五个构造节点是我在整理这套表时新加的，加完之后一次解决了三个原本填不上的案例。

问题是：你交付的是$f_\theta$本身，还是一个包着$f_\theta$的算法$A[f_\theta]$。

传统情形下这个问题没意义。逻辑回归就是逻辑回归，输入进去输出出来，没有中间商。今天不是这样了。

同一个大语言模型，温度设成0和设成1是两种行为。加上思维链是第三种。做best-of-n采样，从n个候选里挑一个，是第四种。接上工具调用和检索，又是一种。

AlphaGo是这个节点最干净的例子。David Silver等人在2016年那篇Nature论文里，策略网络和价值网络只是零件。策略网络负责给出候选着法、把搜索的分支因子压下来，价值网络负责给叶节点打分、省掉大量随机模拟。真正下棋的是蒙特卡洛树搜索。

李世石对面坐的是整个搜索算法。把AlphaGo的神经网络单独拿出来直接输出落子，棋力会掉很多，因为它丢掉了搜索。所以"AlphaGo有多少参数"这个问题回答不了它有多强。

把这一格立成节点，检索增强生成也就有了位置。它改变的既不是函数族也不是参数，改的是推理时那个包装算法，在前向传播之前先去知识库里捞一段文本塞进输入。

这一格还解决了上下文学习，第五章会讲清楚为什么。

有一个后果需要记住：$\theta$不再等于系统。评测一个模型的时候，你评的是$A[f_\theta]$，换个采样温度分数就变了，加上思维链分数又变了。今天很多"模型A比模型B强"的结论，实际比的是两套推理配置。这件事在检验那一块会再出现一次。

定题四个节点、构造五个节点，一共九个，讲完了。

# 三、求解：整张表的重心

## 3.1 「怎么把参数定下来」的七种取值

整张表信息量最大的一格，是求解块的第二个节点：**怎么把参数定下来**。

七种取值，逐个说。

**闭式解**。普通最小二乘和岭回归有解析解，$\hat\theta=(X^\top X)^{-1}X^\top y$，一行公式，代进去就完了。没有迭代，没有学习率，没有随机种子，同样的数据跑一万次得到一模一样的结果。这在今天听起来像上古时代的奢侈，但线性回归至今是绝大多数计量经济学论文的主力工具，理由正是这个确定性。

**一次代数运算**。主成分分析对协方差矩阵做特征分解，取前k个特征向量。跟闭式解的区别在于，这里做的是矩阵分解，不是解方程组。数值上靠的是奇异值分解那一套算法，理论上一步到位，实现上有迭代但那是数值线性代数的内部事务，跟机器学习意义上的"训练"没有关系。

**迭代优化**。这一类里还要分凸和非凸。逻辑回归和支持向量机是凸的，Boser、Guyon和Vapnik在1992年那篇论文里把支持向量机写成一个二次规划的对偶问题，凸问题有唯一全局最优，从哪里出发都收敛到同一个点。神经网络的非凸优化没有这个保证，换个初始化就是另一个解，而这些解的泛化性能可以差很多。

**积分而非优化**。高斯过程和贝叶斯方法不给点估计。它们给的是$\theta$的后验分布，预测的时候对这个后验做积分，输出是一个带不确定性的预测。这里没有"最优参数"这个概念。

这一支的实用价值在小样本场景，贝叶斯优化调超参数就靠它，因为它能诚实地告诉你"这块区域我没试过，不确定性很大"。

**一次写入**。Hopfield在1982年用赫布规则把要记住的模式直接写进权重矩阵，$w_{ij}=\sum_\mu x_i^\mu x_j^\mu$，看一遍数据权重就定了，没有第二遍。这套东西的容量有限，能可靠存储的模式数大约是单元数的0.14倍，超过就开始互相干扰。但它的方法论位置很特别，是全表里唯一一个"零次迭代"的取值。

**随求解生长**。梯度提升树的参数随着轮数增加，每一轮先算当前模型的残差梯度，再长一棵新树去拟合它，树的分裂点靠贪心搜索定下来。这一格跟构造块的"参数化形态"是联动的，参数量和参数值在同一个过程里被同时确定。

**离散搜索**。程序合成走的是这条路。候选程序在一个离散空间里，找法是枚举加剪枝、束搜索，或者把规约写成约束交给SAT/SMT求解器，让验证器检查候选是否满足输入输出的例子。这里没有梯度，因为程序空间上根本没有可微结构，把一个if改成while不存在"改一点点"这回事。

这是第五章补上的第七个取值。

七个取值里，只有第三个跟"训练"这个词沾边。

## 3.2 为什么「训练」不配当节点名

上一节那张清单，就是这篇文章最核心的证据。

如果把这一格命名为"训练"，或者按Domingos的写法命名为"优化"，那么七个取值里有六个变成例外。闭式解不算训练，特征分解不算训练，积分不算训练，一次写入不算训练，生长勉强能算但很别扭，离散搜索完全不算。

一个框架如果对六种主流方法都要打例外，它描述的只是这个学科在某一段时期的主流做法，不是这个学科本身。

这个错误有很强的时代印记。2012年三元组提出的时候，深度学习刚起步，AlexNet那年才拿下ImageNet，主流还是凸优化和统计学习。把这一格叫优化在当时几乎没有代价，因为支持向量机和逻辑回归确实都在做优化。

今天回头看，代价是让整整一代从业者以为"机器学习就是定义损失然后梯度下降"。这个默认假设很难察觉，因为它从来没被明说过，它藏在教材的目录结构里，藏在课程的排课顺序里。

后果是具体的。遇到一个新方法时，第一个问题往往是"它的损失函数是什么"，而这个问题对k近邻、对主成分分析、对高斯过程都不成立。问错问题的代价，是把答案得不出来当成方法有问题。

改名的收益也是具体的。把这一格改成问句之后，"上下文学习的损失函数是什么"这个曾经困扰不少人的问题就消解了：它在这一格上什么都不填，因为参数在这个过程里根本没有被定下来。

## 3.3 两个不同源的时间

接下来两条性质只挂在"迭代优化"这一个取值下面。它们不是节点的性质，选了别的取值就不存在。这个区分很重要，深度学习圈子里很多被当成普遍规律的东西，其实只在这一格成立。

第一条是**两个时间**。

深度方向上，一层一层往前算：

$$h_{\ell+1}=h_\ell+F(h_\ell,\theta_\ell)$$

这里的时间是层号$\ell$。训练方向上，参数一步一步更新：

$$\theta_{t+1}=\theta_t-\eta g_t$$

这里的时间是迭代数$t$。两个时间不同源，一个跑在网络内部，一个跑在训练循环里。加上推理时的算法，也就是构造块第五个节点里那个$A[f_\theta]$，还有第三个时间。

深度那条式子长得像差分方程，很多人也确实把残差网络类比成常微分方程的离散化，Neural ODE那一支工作就建立在这个类比上。但类比要小心，因为这条式子里没有未知量。给定$h_0$，直接往前算就得到$h_L$，不需要求解任何东西。一个真正的差分方程是要解出未知函数的，这里没有什么要解。

更关键的是，它没有$\Delta\ell\to0$的极限可言。把层数加倍并把每层的贡献减半，你得到的是另一个模型，参数量翻倍，表达能力变了，泛化性能也变了。这跟把时间步长减半得到同一个微分方程的更精确解，完全是两回事。

这一点在下一节会变成一条更普遍的性质。

## 3.4 离散性有益，判据整个反过来

第二条性质更有意思。

$\theta_{t+1}=\theta_t-\eta g_t$在形式上是梯度流$\dot\theta=-\nabla L$的显式欧拉格式。学过数值分析的人马上会想到稳定性条件：步长太大就发散，临界点在$\eta\lambda_{\max}\approx2$，其中$\lambda_{\max}$是损失函数Hessian矩阵的最大特征值。

这条边界在深度学习里真的出现了。Jeremy Cohen等人在2021年那篇《Gradient Descent on Neural Networks Typically Occurs at the Edge of Stability》里报告，训练损失的Hessian最大特征值会一路爬升，然后停在略高于$2/\eta$的位置不动，训练损失在短时间尺度上上下震荡，长时间尺度上稳步下降。系统自己跑到了稳定性边界上，然后待在那里。

这跟计算流体里的CFL条件是同一个数学。我读博的时候做磁流体数值模拟，每次时间步长超过CFL限制，程序就在几十步之内炸掉。那种爆炸方式和深度学习里学习率调大之后loss飞掉，屏幕上看到的曲线形状几乎一样，都是先小幅震荡然后指数发散。

相同到此为止。往下走，所有判据都反过来了。

| | 数值积分 | 深度学习 |
|---|---|---|
| 步长 | 误差来源，应该减小 | $\eta\to0$模型反而更差 |
| 噪声 | 应该抑制 | 随机梯度的噪声有益 |
| 两个都收敛的格式 | 一样好，差别叫误差 | 给出不同的模型，差别叫隐式正则化 |

第一行很多人有体感。学习率调得太小，模型确实收敛得更"干净"，训练损失也能降下去，但测试集成绩往往变差。大批量训练要配大学习率，不然泛化会掉，这在工程上已经是常识。

第二行同理。全批量梯度下降在理论上是更准确的下降方向，实践中效果通常不如小批量随机梯度。噪声在这里起的是正则化的作用，帮助跳出尖锐的极小点。

第三行是要害。在数值积分里，龙格库塔和欧拉法只要都收敛，收敛到的是同一个解，差别只是精度。在深度学习里，用SGD和用Adam训练同一个网络，得到的是两个不同的模型，泛化性能也不同，而这个差别恰恰是从业者唯一在意的东西，它有一个专门的名字叫隐式正则化。

原因是这里没有一个"真实解"在等着被逼近。数值格式本身就是那个对象，你换格式，就是换了要研究的东西。这句话在第六章还会回来，它是全文诊断的第三层。

## 3.5 内环：一条独立的轴

求解块的第三个节点问：每一步参数更新，内部还要不要再跑一个循环。

大多数监督学习没有内环。一次前向、一次反向、更新参数，结束。每步的成本可以精确算出来，也可以精确预算。

玻尔兹曼机有。Ackley、Hinton和Sejnowski在1985年那篇《A Learning Algorithm for Boltzmann Machines》里给出的梯度，包含两项，一项对数据分布求期望，一项对模型分布求期望。前一项好算，后一项要求你从模型自己定义的那个分布里采样，而那个分布带着2.5节里算不出来的配分函数$Z$。

唯一的办法是跑MCMC。理论上要跑到马尔可夫链收敛，实践中没人跑得起。Hinton在2002年提出对比散度，从训练样本出发只走一步或几步Gibbs采样，用这个近似的样本去估计后一项。这是让玻尔兹曼机能训练起来的关键一步，代价是梯度有偏，而且偏差随着模型变大越来越难控制。

强化学习也有内环。每次策略更新之前要跟环境交互，跑出一段或者一批轨迹，这就是rollout。在围棋和视频游戏里这个成本可以接受，因为模拟器跑得飞快。在真实机器人上，每一条轨迹意味着一次真实的物理动作，成本高好几个数量级，这是机器人强化学习进展缓慢的直接原因。

这一格的独立性容易被忽略。它跟"怎么把参数定下来"那一格不是一回事。玻尔兹曼机和卷积网络在那一格上都填"迭代优化"，一个有内环一个没有，而这个差别决定了它们的实际训练成本差几个数量级。

第七章会论证，这一格可能是全表里最能预测"一个方法能不能做大"的那一格。

## 3.6 何时停：早停是故意不让优化收敛

第四个节点问**何时停**，答案在不同取值下差别很大。

闭式解不适用，算完就是完，"停"这个概念不存在。凸优化用收敛判据，梯度范数小于某个阈值就停，停下来的地方是全局最优，理论和实践在这里完全对齐。

深度学习用的是完全另一套东西：预算和验证曲线。要么算力花完就停，要么监控验证集损失，一开始回升就停，这就是早停。

值得停下来想一想这件事的性质。早停的意思是，你手上有一个定义清楚的优化问题，你知道再跑下去训练损失还会降，你选择不跑。**你在故意不让优化收敛**。

在数值分析的语境里这是荒谬的。没有人会说"这个方程我故意不解到底，解到一半效果最好"。在深度学习里它是标准操作，写在每一个训练脚本里。

原因回到2.1节那三层代理。你真正想要的$\varepsilon$跟你在优化的$L$不是同一个量。$L$降到底的时候$\varepsilon$早就开始涨了，所以最优策略是在$L$还没降到底的时候停手。早停是三层代理关系在训练循环里的直接后果。

同样的逻辑也解释了为什么正则化有效。权重衰减、dropout、数据增强，这些手段全都在故意让$L$降得慢一点、降得浅一点。整个深度学习的调参实践，很大一部分是在系统性地阻挠优化器完成它的工作。

## 3.7 规模：扫不起，只能外推

第五个节点问算力怎么在参数量、数据量和训练步数之间分配。

这一格在小模型时代根本不存在。你把超参数网格扫一遍，跑几百个实验，挑最好的那个，成本是几台机器几天。到了大模型规模，扫一遍的成本等于把整个项目做十次，于是它从一个工程细节变成了硬约束。

行业的应对方式是外推。在小规模上测出规律，拟合成公式，往目标规模上推。Kaplan等人在2020年给出的结论是最优参数量随算力的0.73次方增长，数据量随0.27次方增长。按这个配比，算力增加时应该把绝大部分投给参数量，推出来的结论是"模型要尽量大"。

DeepMind的Hoffmann等人在2022年的Chinchilla论文里推翻了这个配比。他们的结论是最优参数量随算力的0.50次方增长，参数量和数据量应该等比例放大。按这个标准，当时几乎所有的大模型都严重欠训练，GPT-3的token与参数比只有约1.7，而Chinchilla建议的比例高一个量级。

两篇论文用的是同一套方法，差了两年，结论差了一个量级的资源分配建议。分歧的来源包括学习率调度的处理方式和小规模实验的设计细节，这些细节在外推到三个数量级之外时被放大了。

这一格的困难和别处不同。别处的困难是"不知道怎么算"，这里是"知道怎么算但跑不起"，所以只能预测。一个必须靠外推来做的决策，天然带着不可消除的风险，而且这个风险的量级是几千万美元。

这也是我认为这一格值得单独立出来的理由。它不是"训练步数怎么设"的放大版，它的性质变了：从一个可以试错的工程参数，变成了一个只能赌的战略判断。

# 四、检验：最弱的一块

## 4.1 拿什么比

留出集是最基本的一种。把数据切一刀，一部分训练，一部分测试，测试集上的成绩当作泛化能力的估计。这套做法的合法性建立在一个假设上：两部分数据来自同一个分布，而且测试那部分在训练时完全没被看见。

基准测试是第二种。MMLU、GSM8K、SWE-bench这些名字今天决定着一个模型发布时的头条。它们本质上是被固定下来、被全行业共享的留出集，好处是可比，坏处是公开。

人评是第三种，成本高但对生成类任务几乎无可替代。一篇文章写得好不好，没有任何自动指标能代替人读一遍。配对比较是它最常见的形式，让人从两个输出里挑一个更好的，Chatbot Arena用的就是这套，靠大量匿名对战算出Elo分。

红队是第四种，专门去找失败案例，用在安全评估上。它跟前三种的逻辑相反，前三种测平均表现，红队测最坏情况。一个模型在MMLU上考90分，同时能被一句话诱导出危险内容，这两件事不矛盾。

四种各有各的适用范围，实践中通常混着用。它们有一个共同点，值得单独立一个节点来问。

## 4.2 外部真值几乎恒为「无」

第二个检验节点问：**有没有外部真值**。

意思是，判定模型对错的那个标准，是不是独立于模型和数据集，从外部世界拿来的。

留出集没有。测试集和训练集是同一批数据切出来的，同一个$P$的两组样本。模型在测试集上答对，说明它学到了$\hat P_n$里的规律，至于$\hat P_n$本身对不对、标注有没有系统性偏差、这批数据能不能代表真实世界，测试集一概回答不了。

基准测试也没有，而且更糟，因为它是公开的。前面提到的MMLU污染研究说明了后果：题目进了训练数据，分数就不再测量能力，只测量记忆。这不是可以靠自律解决的问题，只要基准公开、训练数据靠爬取，污染就是默认状态。

人评没有外部真值。标注者本身会错，标注者之间会不一致，而且会系统性地偏向某些风格。长回答比短回答更容易被判为好，格式漂亮的比朴素的更容易赢，这些偏差已经被反复观察到。用人评做训练信号时，模型会去学这些偏差。

红队也没有。它能证明存在失败，不能证明不存在失败。没找到漏洞只说明这一轮没找到。

这一格上，绝大多数机器学习方法填的都是"无"。1.3节说过，全填同一个值的节点通常该合并掉。这一格是例外，因为这个"全填无"本身就是结论：机器学习基本上在**自己给自己判卷**。

## 4.3 AlphaFold：唯一的例外

有一个反例，而且只有这一个够硬。

2020年11月，CASP14评测结果公布。CASP是蛋白质结构预测领域两年一次的盲测，题目是那些结构已经被实验测定、但结果还没有公开的蛋白质。参赛者提交预测，组织方拿实验结构去比对。

AlphaFold2在全部目标上的GDT中位数是92.4分，在最难的自由建模类别（没有已知同源结构可参考）上是87.0分。同期第二名分别是72.8和61.0。这个差距在CASP二十多年的历史上没有出现过。

关键在于评分标准的来源。晶体学和核磁共振测出的那个三维结构，是从物理世界里量出来的。它跟DeepMind的训练数据没有任何关系，跟他们的模型架构没有关系，跟他们的损失函数也没有关系。这是一个真正的外部真值。

这件事的分量在2024年被正式认可，诺贝尔化学奖颁给了David Baker（计算蛋白质设计）以及Demis Hassabis和John Jumper（蛋白质结构预测）。

拿它跟大语言模型的评测对照一下就明白差别在哪。一个模型在MMLU上得90分，意味着它在一套人写的选择题上答对了90%，而这套题可能已经泄漏，而且"人写的选择题"本身就是能力的一个很粗的代理。AlphaFold2得92.4分，意味着它预测的原子位置和X光衍射测出来的位置对得上，误差在埃的量级。

这两个90分不是同一种东西。前者在数据集内部自洽，后者被物理世界验证过。

AlphaFold也提示了这个例外的适用条件。它需要一个独立于机器学习的、成本高但确定的测量手段。这个条件在结构生物学、材料科学、气象预报、芯片设计里存在，因为那些领域有实验室、有卫星、有流片。在"写一封得体的邮件"这类任务上根本不存在，因为那里没有一个物理世界的对照物可以去量。

换句话说，检验这一块的强弱，不取决于机器学习做得好不好，取决于任务本身有没有连着一个可测量的世界。这不是一个可以靠努力解决的问题。

## 4.4 输出回流之后

第三个检验节点问**上线之后分布变不变**。

三种取值。不变是理想情况，几乎不成立。漂移是常见情况，用户行为变了、季节变了、竞品上线了新功能，模型上线三个月成绩掉两个点是家常便饭。

第三种取值是这几年才变成主要矛盾的：模型的**输出回流进训练数据**。

推荐系统里这件事早就存在。模型决定用户看到什么，用户的点击又成为下一轮的训练数据，于是模型在训练自己产生的数据。这个闭环导致的马太效应和信息茧房，讨论了十几年。

语言模型把它推到了极端。互联网上越来越多的文本本身就是模型生成的，下一代模型爬到的语料里混着上一代模型的输出，而且没有可靠的办法区分。合成数据本身可以是有益的，问题在于这个过程不受控，也没人知道现在的比例是多少。

这一格填第三个值的时候，2.1节那个$P$的定义就失效了。$P$不再是一个独立于模型存在的客观分布，它会随着模型的部署而变化。所有"从$P$中独立同分布采样"的理论推导，前提都没了。

这是检验这一块跟前三块最不同的地方。定题、构造、求解三块的决定做完就固定了，检验这一块的对象会随着你的使用而变化。你在测量一个正在被你的测量行为改变的东西。

## 4.5 懂不懂自己造了什么

最后一个节点问：你**懂不懂自己造了什么**。

能力边界在哪，什么输入会让它失败，内部到底在做什么运算。机制可解释性这几年有真进展，用稀疏自编码器在模型内部提取可解释的特征方向，已经能定位出一些对应具体概念的方向。

但相对于模型规模，这些进展覆盖的比例很小。而且"能定位出一个特征"和"能预测这个模型在什么情况下会出错"之间还隔着很远的距离。这一格上，绝大多数情况填"空"。

真正值得注意的是下一句话：这一格空着，**不妨碍前三块完整执行**。你可以在完全不理解一个模型的情况下，定题、构造、求解、上线、赚钱，整条链路一个环节都不缺，商业上也没有惩罚。

这在别的工程学科里不成立。造一座桥而不懂结构力学，桥会塌，而且塌之前不会给你警告。造一台发动机而不懂热力学，效率上不去，竞争对手会赢。造一个模型而不懂它，模型照样跑，用户照样付钱，只是你不知道它什么时候会塌，也不知道塌了以后怎么修。

一个工程学科能在核心环节长期留白而不受惩罚，这个现象本身值得研究。它是第六章诊断的一个入口。

# 五、拿这张表去撞十八个模型

## 5.1 覆盖表

十八个模型，逐个填关键节点。

先看定题与构造这一半。

| 模型 | 函数族 | 通路还是分布 | 推理时算法 |
|---|---|---|---|
| 逻辑回归 | 线性加sigmoid | 通路 | 无 |
| 最小二乘/岭回归 | 线性 | 通路 | 无 |
| 主成分分析 | 线性子空间 | 通路 | 无 |
| 支持向量机 | 核方法的希尔伯特空间 | 通路 | 无 |
| k近邻 | 无参数 | 通路 | 无 |
| 梯度提升树 | 分段常数 | 通路 | 无 |
| 高斯过程 | 核决定的先验 | 分布 | 无 |
| Hopfield网络 | 对称权重能量 | 分布 | 迭代到稳态 |
| 玻尔兹曼机 | 能量函数 | 分布 | MCMC采样 |
| 卷积网络 | 卷积堆叠 | 通路 | 无 |
| 循环网络 | 循环结构 | 通路 | 无 |
| Transformer | 注意力堆叠 | 通路 | 温度采样、思维链 |
| 扩散模型 | 去噪网络 | 分布 | 多步去噪 |
| 强化学习 | 策略网络或价值网络 | 通路 | 有时带搜索 |
| 生成对抗网络 | 生成器加判别器 | 分布 | 无 |
| AlphaGo | 策略网络加价值网络 | 通路 | 蒙特卡洛树搜索 |
| 人类偏好后训练 | 同基座 | 通路 | 采样与打分 |
| AlphaFold | 等变结构加注意力 | 通路 | 循环精修 |

再看求解与检验这一半。

| 模型 | 判据 | 怎么把参数定下来 | 内环 | 外部真值 |
|---|---|---|---|---|
| 逻辑回归 | 交叉熵 | 迭代（凸） | 无 | 无 |
| 最小二乘/岭回归 | 平方误差 | 闭式解 | 无 | 无 |
| 主成分分析 | 重构误差 | 特征分解 | 无 | 无 |
| 支持向量机 | 间隔最大 | 凸二次规划 | 无 | 无 |
| k近邻 | 无 | 不求解 | 无 | 无 |
| 梯度提升树 | 任意可微损失 | 随求解生长 | 无 | 无 |
| 高斯过程 | 边际似然 | 积分，非优化 | 无 | 无 |
| Hopfield网络 | 存储容量 | 一次赫布写入 | 无 | 无 |
| 玻尔兹曼机 | 似然 | 迭代 | MCMC | 无 |
| 卷积网络 | 交叉熵 | 迭代（SGD） | 无 | 无 |
| 循环网络 | 交叉熵 | 迭代（时序反传） | 无 | 无 |
| Transformer | 下一token预测 | 迭代（Adam） | 无 | 无 |
| 扩散模型 | 去噪得分匹配 | 迭代 | 无 | 无 |
| 强化学习 | 累积回报 | 迭代（策略梯度） | 环境交互 | 无 |
| 生成对抗网络 | 纳什均衡 | 交替更新 | 无 | 无 |
| AlphaGo | 累积回报 | 迭代加自我对弈 | 自我对弈 | 无 |
| 人类偏好后训练 | 人类偏好 | 迭代 | 采样 | 无 |
| AlphaFold | 结构误差 | 迭代 | 无 | **有：CASP盲测** |

十八行填完，没有一格填不上。最后一列十八行里有十七行是"无"。

这十八个怎么选的。取法是：把"怎么把参数定下来"那一格的七个取值各取一到两个代表，保证七个取值全部被覆盖。再补上过去十年真正在产业里跑起来的那几个，避免这张表变成一张只有老古董的清单。按这个取法，被排除在外的主要是各种变体，比如各类正则化的线性模型、各种注意力变种，它们跟表里已有的行填的值完全一样，加进来不增加信息。

## 5.2 三个真把表撞出坑的案例

上一节那张表是安全区，全是我立表时脑子里想着的那些方法。真正的检验是拿它去撞我没想到的东西。

撞了五个，三个撞出了坑。

**上下文学习**。GPT-3在2020年展示的能力：在prompt里给几个例子，模型就能做一个它没被专门训练过的任务，全程没有任何参数更新。这件事当时冲击很大，因为它看起来像"学习"，但按照任何传统定义它都不是学习。

拿表去填，"怎么把参数定下来"这一格填不上，因为在这个过程里根本没有参数被定下来。

这一撞的结论是表没缺节点，缺的是我原来没把构造块第五个节点（交付的是$f_\theta$还是$A[f_\theta]$）立出来。上下文学习发生在$A$里面，具体说是发生在一次前向传播内部。参数早在预训练时就定死了，prompt改变的是输入，不是$\theta$。

反过来说，这个案例恰好证明第五个节点必须独立存在。没有它，上下文学习在表上无处安放，而这正是当年那批"上下文学习是不是隐式梯度下降"讨论的来源：大家试图用求解块的语言去描述一个发生在构造块的现象。

**程序合成**。给一组输入输出的例子，让系统写出一个满足这些例子的程序。微软的FlashFill是最广为人知的产品化例子，Excel里那个你填两行它就自动补全整列的功能就是它。

拿表去填，"怎么把参数定下来"的原有六个取值一个都不对。这里没有参数向量，有的是一个离散的程序空间。找法是枚举加剪枝，或者把规约写成约束交给SAT/SMT求解器，让求解器去找满足约束的程序。

这一撞是真缺一个取值。补上第七个：**离散搜索**。补完之后，进化算法、遗传编程、神经架构搜索里的非梯度变体也都有地方放了。

**贝叶斯非参数方法**。狄利克雷过程混合模型的定义特征是参数量不预先设定，随着观测数据增多而增长，聚类数可以一直长到样本量级。你不需要事先告诉它数据里有几个簇。

拿表去填，"参数化形态"那一格的三个取值不够用。它不是固定的，也不是无参数的。跟梯度提升树的"随求解生长"很像但不一样：梯度提升树的参数量由你跑多少轮决定，是求解过程的函数。狄利克雷过程的参数量由看到多少数据决定，是数据量的函数。

同样的数据跑一百轮和跑一千轮，狄利克雷过程给出的有效簇数不变。

补上第四个取值：**随数据生长**。

另外两个撞过来的没撞出坑。神经符号方法在函数族那一格填"混合"，其余照填，它的特殊之处在于把两种函数族拼起来，而拼接本身不是新问题。检索增强生成落在构造块第五个节点里，跟思维链同一格。

## 5.3 补丁之后

三处修改，写在一起：

| 节点 | 原有取值 | 补丁 | 触发案例 |
|---|---|---|---|
| 构造·参数化形态 | 固定／随求解生长／无参数 | 加**随数据生长** | 狄利克雷过程 |
| 构造·交付的是什么 | 原本没这一格 | 立为独立节点 | 上下文学习、AlphaGo、检索增强 |
| 求解·怎么把参数定下来 | 六个取值 | 加**离散搜索** | 程序合成 |

写这一节的时候我想过要不要把撞坑的过程藏起来，直接给一张完整的表，宣称"无空格、无例外"。

不藏的理由有两个。一是那样不诚实，这三处确实是被撞出来的，不是设计出来的。二是撞坑的过程本身就在演示1.3节那条合格标准：一张好表被反例撞到时，反例会告诉你少了哪一格。三个案例，两次告诉我缺取值，一次告诉我缺节点，一次都没有让我想去质疑那个反例本身。

反过来说，如果哪天有个反例撞过来，我的第一反应是"这东西不算机器学习"，那才是这张表该被扔掉的信号。

## 5.4 从表里读出来的三条规律

表填完，有三件事是从表里直接看出来的，不是我事先想好的。

求解块第二个节点（怎么把参数定下来）的取值分布严重不均。十八行里十一行填的是"迭代优化"的某种变体。这一格上另外六个取值加起来只覆盖七行，而且这七行里有五行是1970年之前或者1980年代提出的方法。一个有七个取值的节点，实际使用高度集中在一个值上，这本身是关于这个学科当下状态的一个事实。

构造块第四个节点（通路还是分布）跟求解块第三个节点（有没有内环）强联动。填"分布"的五行里，玻尔兹曼机训练时要MCMC，Hopfield网络推理时要迭代到稳态，高斯过程要做积分，都逃不掉一个额外的循环。唯一的例外是扩散模型，它填了"分布"却没有训练内环。这个例外是第七章的主角。

检验块第二个节点（有没有外部真值）十八行里十七行填"无"。这一条前面已经说过，放在这里是因为只有把整张表铺开，这个比例才有冲击力。十八分之一。

## 5.5 什么不在表内

有两个东西经常被拿来跟上面这些并列，但它们不该进表。

伊辛模型是物理模型，描述的是磁性材料里自旋之间的相互作用。玻尔兹曼机借用了它的数学形式，两者的能量函数长得几乎一样。区别在于，伊辛模型不学习任何东西，它没有数据、没有判据、没有参数拟合，它是关于磁铁的一个物理假设，可以被实验证伪。这个区别恰好预示了第六章要讲的事。

玻尔兹曼方程是动理学方程，描述稀薄气体里分布函数随时间的演化。它跟玻尔兹曼机除了共用一个人名之外没有关系。它不在这张表里，但它在第六章有一个很重要的位置，作为对照物出现。

# 六、诊断：中间少了一层

## 6.1 什么才算一个方程

前面五章是描述，从这里开始是判断。

一个成熟科学的核心，通常是一个方程。牛顿第二定律、麦克斯韦方程组、薛定谔方程、纳维-斯托克斯方程、爱因斯坦场方程，学科的名字可以不同，这个位置上总有一个方程。它是学科的锚点，教科书围着它组织，研究围着它展开。

什么样的东西配叫方程，我用三条标准：

**先于解法存在**。纳维-斯托克斯方程在1845年就写出来了，数值求解要等一百多年后有计算机。方程和解法是两件事，方程在前，而且方程在没有解法的一百年里依然是有意义的知识。

**独立于解法**。同一个方程可以用有限差分解、用有限元解、用谱方法解，解出来的是同一个东西。换解法不换对象。这条保证了"方程"是一个客观存在，不是某种算法的副产品。

**可以是错的**。这条最关键。牛顿力学在高速下错了，被相对论替换。一个方程是对世界的一个可错的猜想，实验可以否证它，而且历史上确实反复否证过。

第三条是分界线。一个不可能错的东西，不是关于世界的陈述，它是定义或者约定。这个区分在下一节会派上用场。

## 6.2 四层逐一查

成熟科学在方程周围通常有四个层次。拿机器学习逐层去比。

**微观运动方程这一层**。物理有，而且是可错的猜想。机器学习这一层上有的是算法。函数族是我们自己规定的，怎么把参数定下来也是我们自己规定的，两者都不可能"错"。

这一点值得说透。你可以说某个架构效果不好，但没法说它是假的。"Transformer是错的"这句话没有意义，因为Transformer不是关于世界的一个断言，它是一个我们写下来的计算规则。相比之下，"电子的行为遵守薛定谔方程"是一个断言，可以被实验推翻。

这一层是空的。

**变分原理这一层**。物理有，最小作用量原理是很多方程的来源。机器学习也有，求解块第一个节点（判据是什么）填的损失函数、似然、累积回报，形式上就是变分原理，都是"让某个泛函取极值"。

差别在下一步。在物理里，变分原理经过欧拉-拉格朗日方程转成微分方程，然后你研究那个方程，方程才是研究对象。在机器学习里，判据写下来之后直接送进求解器硬算，中间那个"转成方程"的步骤从来没有发生过。没有人写下过深度学习的欧拉-拉格朗日方程再去研究它的解的性质。

**数值格式这一层**。物理里数值格式是对方程的近似，误差应该减小，格式的好坏由它逼近真解的程度决定。机器学习里，3.4节那张表已经说清楚了，格式本身就是那个对象。$\eta\to0$给出的是一个更差的模型，精度在这里根本不是评价维度。

这一层的性质整个变了。

**宏观状态方程这一层**。物理有，理想气体状态方程$PV=nRT$，而且能从微观的分子运动论推导出来。机器学习也有一样东西站在这个位置上：标度律。

| 层 | 成熟科学 | 机器学习 |
|---|---|---|
| 微观运动方程 | 有，是关于对象的可错猜想 | 空。有的是算法，我们自己规定的 |
| 变分原理 | 有，经欧拉-拉格朗日转成方程 | 有，但从不转成方程，直接硬算 |
| 数值格式 | 对方程的近似，误差应减小 | 格式本身就是对象 |
| 宏观状态方程 | 有，且可从微观导出 | 有（标度律），纯经验，未被导出 |

四行里，最上面那行是空的，最下面那行有东西但来路不明。中间两行都在，但都被抄了近路。

## 6.3 玻尔兹曼方程是怎么造出来的

"方程层长什么样"这句话有点抽象，给一个具体样本。

从最底下开始。一盒气体，$N$个分子，严格的描述是刘维尔方程，它描述$6N$维相空间里概率密度的演化，每个分子三个位置坐标三个动量坐标。这个方程完全正确，也完全没用，因为$N$是$10^{23}$量级，这个相空间大到无法想象。

这个处境跟机器学习很像：底层规则完全已知，写得出来，但直接用它什么也算不出来。物理学家没有停在这里。

往上一层是BBGKY层级，以Bogolyubov、Born、Green、Kirkwood、Yvon五个人命名。做法是只看一个分子的分布函数，把其余分子的影响积掉。结果是一个套一个的方程链：单分子分布的方程里出现双分子分布，双分子分布的方程里出现三分子分布，一直到$N$。

层级本身没解决问题，它只是把困难换了个形式。真正的一步是**闭合**：假设两个分子在碰撞之前速度不相关，这就是玻尔兹曼在1872年用的分子混沌假设。这个假设一加，链条在第一层就断了，得到一个自洽的方程，玻尔兹曼方程。

这一步是全过程的关键，也是全过程唯一可错的地方。分子混沌假设推不出来，它是一个额外的物理输入。而且它引入了时间箭头：刘维尔方程时间反演对称，玻尔兹曼方程不对称，H定理说熵只增不减。一个对称的底层，加一个看起来无害的假设，得到一个不对称的宏观规律，这件事在物理学史上引发过长达几十年的争论。

代价有多大，数学上有确切的答案。Oscar Lanford在1975年给出了严格的推导，条件是硬球模型和Boltzmann-Grad极限，结论只在很短的时间内成立，大约是平均自由时间的五分之一。也就是说，这个被物理学家用了一百五十年的方程，严格意义上被证明的适用范围窄得可怜。

我要强调的不是这套推导有多完美，恰恰相反，它有明确的漏洞和明确的适用范围。这正是一个**方程层该有的样子**：它可错，它的错法你说得清楚，它的适用边界能被数学证明或者被实验测出来。你知道什么时候可以信它，什么时候不能。

机器学习缺的就是这样一样东西。从"每个参数按梯度更新"到"损失随算力按幂律下降"，中间没有任何一步类似于BBGKY加闭合的推导。我们有底层规则，我们有宏观现象，中间那条推导路径是空白的。

## 6.4 降维手段都作用于方程

缺这一层的后果，比"看起来不够优雅"严重得多。

物理学处理复杂系统的全部武器，微扰论、平均场、变分法、重整化群、标度分析，无一例外都是作用在方程上的操作。微扰论是把方程拆成可解部分加一个小项。平均场是把方程里的涨落项换成平均值。

重整化群是研究方程在改变尺度时如何变形。变分法是在一族试探解里找最接近方程真解的那个。

没有方程，这些工具就没有可以作用的对象。你不能对一段Python代码做微扰展开。

这解释了一件常被抱怨的事：为什么大量物理学家转行做机器学习之后，觉得自己那套本事使不上劲。我自己转行的时候也有这个体验，读博六年练出来的东西，能直接用上的主要是数值计算的手感和调试大规模程序的耐心，那套解析工具几乎全部闲置。那套工具本身没问题，缺的是一个能接住它们的接口。

机器学习目前只有两头。底下是算法，一堆我们自己规定的、不可能错的规则。顶上是标度律，一条从实验里拟合出来的经验曲线。中间是空的。

所以，**缺的是"方程层"**，不是"近似层"。近似层的缺失是一个推论：没有方程，也就没有什么可以被近似。这个先后关系值得说清楚，因为很多讨论停在"深度学习缺理论"这个笼统的判断上，而缺什么理论、缺在哪一层，答案不一样，接下来该做的事也不一样。

## 6.5 标度律是状态方程，不是运动方程

6.2节那张表的最后一行需要定调：标度律算不算方程。

按6.1节那三条标准查。它先于解法存在吗？不算，它是从大量实验里拟合出来的，没有独立于实验的来源。它独立于解法吗？不。Kaplan和Chinchilla两组指数的差别，很大程度来自实验设计和学习率调度的差异，换一套实验方案就换一组指数。

它可以是错的吗？可以，而且已经被修正过一次。

三条里过了一条。所以标度律当不了运动方程。

但它也不是什么都不是。它是一条**状态方程**：把宏观可观测量联系起来的经验关系。这个位置在物理里是有先例的，而且先例的历史很长。

Robert Boyle在1662年测出气体压强和体积成反比。这条定律纯粹是经验的，来自反复测量，没有任何微观理论支撑，因为那时候连原子论都还是哲学猜想。理想气体状态方程$PV=nRT$在十九世纪上半叶已经在工程上大量使用，而它从分子运动论被推导出来，要等到麦克斯韦和玻尔兹曼，中间隔了两百年。

所以机器学习今天的处境有一个历史对应物：手里有玻意耳定律，没有分子运动论。

这个类比比"什么都没有"更强，也更奇怪。物理的历史顺序是先有宏观经验定律，后有微观方程，然后把前者从后者推出来。这个顺序在机器学习这里走不通，因为微观那一层不是"还没找到"。

那一层已经被填满了，填的是我们自己写的算法。而算法不可能被证伪，所以那个位置上没有空间留给一个可错的方程。

这是两者真正的不同。玻意耳当年不知道分子长什么样，但分子确实在那里，等着被发现。机器学习底下那一层站着的是人造物，人造物不需要被发现。

至于那个位置上到底能不能放进一个可错的东西，我的判断是不清楚。有一种可能是，机器学习的"微观规律"根本不该到算法层去找，而该到数据的统计结构里去找，因为真正决定模型行为的是数据分布的性质，算法只是把它读出来的工具。这个方向上已经有一些工作，但离一条能写下来的方程还很远。

# 七、一个反相关，一个可以被证伪的判据

## 7.1 有方程的那一支，都边缘化了

回头看第五章那两张表的几列，有一件事很扎眼。

真的有"方程层"的那一支：玻尔兹曼机、Hopfield网络、各类能量模型。它们定义的是一个概率分布，有明确的能量函数，有配分函数，整套统计物理的语言可以直接用上去。自旋玻璃理论、replica方法、平均场近似，这些工具在这一支上是现成能用的。

精确可解的那一支：最小二乘、主成分分析、支持向量机、高斯过程。它们有闭式解或者凸对偶，随机矩阵理论、凸分析在这里都有用武之地，泛化误差可以被写成公式而不是画成曲线。

这两支在今天的产业里，基本都退到了边缘。翻一遍2026年的主要模型发布，能量模型的份额接近零，凸方法主要活在特征工程的预处理环节。

这件事有一个很尖锐的注脚。2024年的诺贝尔物理学奖颁给了John Hopfield和Geoffrey Hinton，理由是"为用人工神经网络进行机器学习奠定基础的发现与发明"。Hopfield得奖靠的是1982年那个联想记忆网络，Hinton得奖靠的是1980年代中期用统计物理工具造出来的玻尔兹曼机。

拿物理学奖的正是这一支。而在生产环境里大规模跑着的，是另一支。

我第一次注意到这个对应关系是在给团队做内部分享的时候，把奖项时间线和我们线上实际部署的模型清单并排放在同一页上，那个错位很难忽略。物理气质最重的方法拿了物理学奖，同时在生产环境里几乎见不到。

反相关是真的。它需要一个解释，而"物理没用"这个解释马上会被下一节推翻。

## 7.2 扩散模型这个反例

有一个模型把这个反相关打破了。

扩散模型满身都是物理。名字来自非平衡热力学的扩散过程，前向过程是往数据里一步步加高斯噪声直到变成纯噪声，这跟布朗运动是同一套描述。训练目标是得分匹配（score matching），采样过程和朗之万动力学是同一套数学。

Jonathan Ho、Ajay Jain和Pieter Abbeel在2020年那篇DDPM论文里说得很明确，他们的训练目标是通过"扩散概率模型与去噪得分匹配加朗之万动力学之间的一个联系"设计出来的。这不是事后附会的物理类比，物理是设计的输入。

它赢了。今天几乎所有的图像和视频生成都在用它，Stable Diffusion、Midjourney、Sora这一整条线全在这个框架下。

而且它在第五章那张表上明确填了"分布"。它跟玻尔兹曼机站在同一格里，面对同一个配分函数，结局完全相反。

所以"有物理就输"这个说法不成立。反相关是真的，但它的原因在别的地方。

## 7.3 差别在内环

把扩散模型和玻尔兹曼机放在一起对比，差别只在一格上，就是求解块第三个节点（有没有内环）。

玻尔兹曼机的梯度里带着配分函数$Z$。每算一次梯度，都要从模型分布里采样，也就是要跑一段MCMC。Hinton在2002年的对比散度把马尔可夫链砍到只走一步，训练才勉强能跑，代价是梯度有偏。这个方法在小规模上有效，往大了做，偏差和采样成本一起爆炸，链子需要走的步数随着模型变大而增加，而你能给的步数只会变少。

扩散模型面对的是同一个$Z$，处理方式完全不同。Aapo Hyvärinen在2005年证明，得分函数，也就是对数密度的梯度$\nabla_x\log p(x)$，跟归一化常数无关。原因很简单：

$$\nabla_x\log\frac{e^{-E(x)}}{Z}=\nabla_x\big(-E(x)-\log Z\big)=-\nabla_x E(x)$$

$\log Z$不含$x$，求导直接消掉。于是训练目标可以只针对得分函数写，$Z$从头到尾不出现。

DDPM里那个训练目标最后化简成什么？给一张图加上已知强度的高斯噪声，让网络预测加进去的是哪个噪声，损失是均方误差。一个普通的回归任务，每一步的成本跟训练一个图像分类器没有区别。

这是关键的一步。同样是能量模型这一支，同样面对配分函数，玻尔兹曼机把采样放进了训练的内环，扩散模型把采样全部推到了推理阶段。训练时是纯回归，采样时才跑几十步去噪，而那时候不需要梯度，也不需要反向传播。

成本结构完全不同。前者的每一步训练都被采样质量绑住，后者的每一步训练都可以吃满硬件。

## 7.4 判据，以及杀死这条判据的三种办法

由此得到这篇文章唯一的一条原创主张：

> 决定成败的那一格，是**内环里要不要采样**。

一个方法能不能做大，很大程度上取决于求解块第三个节点（有没有内环）填的是什么。内环里带采样或者带昂贵的环境交互，每一步梯度的成本就跟采样质量绑死，规模一上去就撑不住。内环为空，训练就是纯粹的前向加反向，可以直接吃硬件红利。

这条判据能解释的不只是扩散模型。强化学习内环里要跟环境交互，这解释了为什么它在有廉价模拟器的领域（棋类、电子游戏）跑得动，在真实机器人上一直很慢。生成对抗网络没有采样内环，所以它在2014年之后几年迅速铺开，后来被扩散模型取代是因为训练不稳定，跟内环无关。人类偏好后训练有一个采样内环，成本确实高，但采样的是自己的输出而不是MCMC，走一遍前向就够，所以还撑得住。

一条主张要有价值，得说清楚什么情况下它是错的。这条判据的证伪条件有三个：

**跑赢的内环方法出现**。一个内环带昂贵采样的方法，在前沿规模上压过无内环的方法。 如果某种带持续MCMC的能量模型在图像生成上超过扩散模型，判据就死了。

**归因被推翻**。有人证明扩散模型的成功跟内环无关。 做一个消融实验，把得分匹配换成一个需要采样的等价目标，其余全不动，如果结果差别不大，说明我归因错了。

**采样变便宜**。硬件或算法把采样成本降到可以忽略。 如果专用采样硬件或者更好的采样算法让MCMC的成本降到跟一次前向传播同量级，这条判据就失去了预测力。它本来就是一条关于成本结构的经验判断，成本结构变了，结论就该重算。

第三条最有可能发生，也最值得盯着。

要公平地说，这条判据也有人会不同意。一种反对意见是，玻尔兹曼机的失败主要来自表达能力有限和深层结构难以训练，采样成本只是众多原因之一，1980年代那一支同时被好几个问题卡住，把账全算到内环上是一种简化。

这个反对有道理，我接受它的事实部分。我保留这条判据，是因为它给出了可检验的预测，而"有很多原因"给不出任何预测。一条可能被推翻的判据，比一段永远正确的描述有用。

## 7.5 这篇文章没做的

三件事明确留在外面。

没有解释**注意力为什么有效**。构造块第一个节点（候选函数是哪一堆）在本文里只被列出来，没有被解释。为什么注意力堆叠比循环结构更适合语言，这是一个真问题，这张表回答不了。表告诉你有这一格，不告诉你该往里填什么，这是节点表这种东西的固有边界。

检验块第四个节点（懂不懂自己造了什么）恒空这件事，值得单独写一篇。4.5节说过，这一格空着不妨碍前三块完整执行。一个工程学科能在核心环节长期留白而不受惩罚，这个现象本身比留白的内容更值得研究。

**物理那一层没有进来**。这篇只写机器学习自己。逐个节点去问"这一格的困难是什么类型的困难，有没有现成的知识体系专门处理这类困难"，是另一篇文章的事。第六章那个诊断给了它一个入口，也给了它一个警告：在方程层被填上之前，物理工具找不到着力点。

最后回到开头那个问题。主成分分析和Transformer在方法论上算不算同一类东西。

答案是，它们在十八个节点上填不同的值，其中最要紧的那一格，一个填特征分解，一个填迭代优化。它们是同一张表上的两行。

有意思的地方在于，把这张表整个铺开之后，最显眼的地方并不在任何一行上，而在纵向那四层结构最上面一行的空白里。我们有算法，有从实验里拟合出来的标度律，中间那一层从来没有人写出来过，也不清楚它是不是原则上就写不出来。

玻意耳测出他那条定律之后，等了两百多年才等到分子运动论。区别在于，他不知道分子长什么样，但分子在那里。

---

## 作者其它文章（选）

- [AI for Physics全景图（长篇科普）](https://x.com/snowboat84/status/2085946919277461512)
- [什么是MoE和MMoE模型？](https://x.com/snowboat84/status/2085586981459828939)
- [自动驾驶的十年：那些空头支票是怎么开出来的](https://x.com/snowboat84/status/2085206937462002072)
- [量子力学研究史（中）：场、无穷大与标准模型](https://x.com/snowboat84/status/2084791538933194873)
- [FDE介绍和入门：一个被AI重新激活的老岗位](https://x.com/snowboat84/status/2084428243512537562)
- [大数据概念兴衰史](https://x.com/snowboat84/status/2083346249663844656)
- [量子力学研究史（上）：从黑体辐射到希尔伯特空间](https://x.com/snowboat84/status/2083063168981737698)
- [纳维-斯托克斯方程：一个方程背后的多重难题](https://x.com/snowboat84/status/2082618473776329127)
- [算法交易是怎么吃掉华尔街的](https://x.com/snowboat84/status/2082257654353895482)
- [顶尖VC 2026年以来动向观察](https://x.com/snowboat84/status/2081903835833147732)
- [一篇文章讲清楚美国的移民系统](https://x.com/snowboat84/status/2057980486501433383)
- [一文讲清楚美国医疗系统](https://x.com/snowboat84/status/2055081426744422697)
- [细说美国的华人老钱家族](https://x.com/snowboat84/status/2062326581776011623)
- [美国的犹太人和华人分别抢到了什么资源？详细分析](https://x.com/snowboat84/status/2063049247805837815)
- [一篇文章看懂美国教育全生态](https://x.com/snowboat84/status/2054359249917210633)
- [AI圈大V名单（名单不断扩充中）](https://x.com/snowboat84/status/2069206740546343372)
- [什么是控制论？控制论是AI的上辈子吗？](https://x.com/snowboat84/status/2064496706042069340)
- [祖父积分学概论](https://x.com/snowboat84/status/2056533111983493136)
- [美国数学教育和研究现状](https://x.com/snowboat84/status/2080872418999156980)
- [教宗良十四世论人工智能（精华版）](https://x.com/snowboat84/status/2059434342745866391)

---

## 本文参考文献

- [A Few Useful Things to Know about Machine Learning](https://datajobs.com/data-science-repo/Machine-Learning-Overview-%5BPedro-Domingos%5D.pdf) - Pedro Domingos, CACM 2012，表示／评价／优化三元组
- [Machine Learning](https://www.cs.cmu.edu/~tom/mlbook.html) - Tom Mitchell 1997，T/P/E定义
- [CRISP-DM: Towards a Standard Process Model for Data Mining](https://cs.unibo.it/~danilo.montesi/CBD/Beatriz/10.1.1.198.5133.pdf) - Wirth & Hipp 1999
- [Statistical Modeling: The Two Cultures](https://projecteuclid.org/journals/statistical-science/volume-16/issue-3/Statistical-Modeling--The-Two-Cultures-with-comments-and-a/10.1214/ss/1009213726.full) - Leo Breiman, Statistical Science 2001
- [The Lack of A Priori Distinctions Between Learning Algorithms](https://www.semanticscholar.org/paper/The-Supervised-Learning-No-Free-Lunch-Theorems-Wolpert/689c74bd55e0be2b0866b9a86ec53363ce82aed1) - David Wolpert 1996，无免费午餐定理
- [The Bitter Lesson](http://www.incompleteideas.net/IncIdeas/BitterLesson.html) - Richard Sutton 2019
- [On Lines and Planes of Closest Fit to Systems of Points in Space](http://www.stats.org.uk/pca/) - Karl Pearson 1901，主成分分析起源
- [Analysis of a Complex of Statistical Variables into Principal Components](http://cda.psych.uiuc.edu/hotelling_principal_components.pdf) - Harold Hotelling 1933
- [Nearest Neighbor Pattern Classification](https://isl.stanford.edu/~cover/papers/transIT/0021cove.pdf) - Cover & Hart, IEEE Trans. Inf. Theory 1967
- [A Training Algorithm for Optimal Margin Classifiers](https://www.scirp.org/reference/referencespapers?referenceid=2865895) - Boser, Guyon & Vapnik, COLT 1992
- [Greedy Function Approximation: A Gradient Boosting Machine](https://projecteuclid.org/journals/annals-of-statistics/volume-29/issue-5/Greedy-function-approximation-A-gradient-boosting-machine/10.1214/aos/1013203451.full) - Jerome Friedman, Annals of Statistics 2001
- [Neural Networks and Physical Systems with Emergent Collective Computational Abilities](https://doi.org/10.1073/pnas.79.8.2554) - John Hopfield, PNAS 1982
- [A Learning Algorithm for Boltzmann Machines](https://onlinelibrary.wiley.com/doi/abs/10.1207/s15516709cog0901_7) - Ackley, Hinton & Sejnowski, Cognitive Science 1985
- [Training Products of Experts by Minimizing Contrastive Divergence](https://www.cs.toronto.edu/~hinton/absps/nccd.pdf) - Geoffrey Hinton 2002，对比散度
- [Estimation of Non-Normalized Statistical Models by Score Matching](https://www.jmlr.org/papers/v6/hyvarinen05a.html) - Aapo Hyvärinen, JMLR 2005
- [Generative Adversarial Networks](https://arxiv.org/abs/1406.2661) - Goodfellow et al. 2014
- [Mastering the Game of Go with Deep Neural Networks and Tree Search](https://www.nature.com/articles/nature16961) - Silver et al., Nature 2016，AlphaGo与蒙特卡洛树搜索
- [Language Models are Few-Shot Learners](https://arxiv.org/abs/2005.14165) - Brown et al. 2020，上下文学习
- [Denoising Diffusion Probabilistic Models](https://hojonathanho.github.io/diffusion/) - Ho, Jain & Abbeel, NeurIPS 2020
- [Scaling Laws for Neural Language Models](https://arxiv.org/abs/2001.08361) - Kaplan et al. 2020
- [Training Compute-Optimal Large Language Models](https://arxiv.org/abs/2203.15556) - Hoffmann et al. 2022，Chinchilla
- [Gradient Descent on Neural Networks Typically Occurs at the Edge of Stability](https://ui.adsabs.harvard.edu/abs/2021arXiv210300065C/abstract) - Cohen et al. 2021
- [Applying and Improving AlphaFold at CASP14](https://pubmed.ncbi.nlm.nih.gov/34599769/) - Jumper et al. 2021，CASP14成绩
- [The Nobel Prize in Chemistry 2024](https://www.nobelprize.org/prizes/chemistry/2024/press-release/) - 诺贝尔奖官方，Baker／Hassabis／Jumper
- [The Nobel Prize in Physics 2024](https://www.nobelprize.org/prizes/physics/2024/press-release/) - 诺贝尔奖官方，Hopfield／Hinton
- [A Survey on Data Contamination for Large Language Models](https://arxiv.org/html/2502.14425v2) - 2025，基准污染综述
- [LLM Benchmark Contamination: MMLU Data Leakage](https://blog.pebblous.ai/blog/llm-benchmark-contamination/en/) - MMLU 29.1% 污染率与GSM8K掉分数据
- [The Rigorous Derivation of the Boltzmann Equation](https://www.math.u-bordeaux.fr/~pthieull/LIA/Events/2021/PDF_Files/Dolmaire_Theophile.pdf) - Lanford 1975结果与BBGKY层级综述
- [Goodhart's Law](https://en.wikipedia.org/wiki/Goodhart%27s_law) - Goodhart 1975原始表述与Strathern 1997改写
- [A Tutorial on Bayesian Nonparametric Models](https://arxiv.org/pdf/1106.2697) - Gershman & Blei 2011，狄利克雷过程与随数据生长
- [A Comparative Review of Program Synthesis Paradigms](https://arxiv.org/pdf/2508.00013) - 2025，枚举搜索与SAT/SMT约束求解

---

## 附录：原始草稿

> **标题候选**（未定）：①机器学习的方法论骨架（现用）②机器学习的必答问题 ③机器学习的决策节点。 「范式」一词已弃用：库恩意义上的范式指共同体共享的预设，本文做的是流程分解；且中文 ML 语境中「范式」已被「预训练范式」一类占用，会误导读者预期。
>
> 底稿第一层：只写 ML 自身，**刻意不提物理**。物理叠加位见最后，现为空。
>
> **写法约定**：节点是**必须回答的问题**，不是某一类模型给出的答案。 上一版把第三块写成"推进"，那是梯度训练的答案，于是 PCA、k-NN、高斯过程一来就"不符合"。 它们不是不符合节点，是在同一个节点上填了不同的值。全部改成问句之后没有例外。
>
> ## 符号
>
> $W$ 想要的东西 → $M$ 指标 → $L$ 判据。优化的是 $L$，评价的是 $M$，想要的是 $W$。 $\theta$ 参数｜$f_\theta$ 函数｜$h_\ell$ 第 $\ell$ 层表示｜$P$ 真实分布（不可观测）｜$\hat P_n$ 数据集｜$\varepsilon$ 真实风险（不可计算）
>
> ## 四大块
>
> |块|要回答的问题|
> |---|---|
> |**一　定题**|要什么、化成什么可测量、拿什么数据|
> |**二　构造**|用什么装、塞什么先验、参数化是什么形态|
> |**三　求解**|判据是什么、**怎么把参数定下来**、何时停、规模怎么分|
> |**四　检验**|拿什么比、有没有外部真值、上线后如何、懂不懂自己造了什么|
>
> ## 一　定题
>
> |节点|问题|可能的取值|
> |---|---|---|
> |1.1|想要什么 $W$？|—|
> |1.2|化成什么可测量 $M$？|准确率／BLEU／胜率／pass@k／结构误差|
> |1.3|拿什么数据？|来源、规模、许可、去重、配比、切分、表征|
> |1.4|**目标与数据谁先？**|目标先（监督学习）／**数据先，目标后定**（自监督预训练、基础模型）|
>
> 1.4 必须是独立节点：基础模型的实际顺序是先有数据 → 造代理任务 → 目标事后由下游定，**跟 1.2→1.3 的顺序是倒的**。 这一块的决定最不可逆、最没有理论指导。$\hat P_n$ 一旦冻住就成了后面全部计算的系数。 **故障**：Goodhart（$M$ 涨而 $W$ 未涨）、测试集污染、泄漏。
>
> ## 二　构造
>
> |节点|问题|可能的取值|
> |---|---|---|
> |2.1|用什么函数族装？|线性／核 RKHS／分段常数（树）／卷积堆叠／循环／注意力堆叠／能量函数／无参数|
> |2.2|塞什么归纳偏置？|对称性（平移／置换／旋转等变）、局域性、因果掩码、层级、稀疏|
> |2.3|参数化是什么形态？|**固定**（神经网络）／**随求解生长**（GBDT、NAS）／**无参数**（k-NN）|
> |2.4|是一条通路还是一个分布？|**通路**：$f_\theta$ 直接算出｜**分布**：$p\propto e^{-E/T}$，用它必须采样|
>
> 2.3 与 2.4 是上一版完全缺的两个节点，也是上一版套不住树模型和玻尔兹曼机的原因。 2.4 取"分布"时，会引出第三个时间 $\tau$（采样时间），且它可能落在求解的**内环**里 —— 见 3.3。
>
> ## 三　求解
>
> |节点|问题|可能的取值|
> |---|---|---|
> |3.1|判据是什么？|损失极小／似然／**均衡**（GAN）／**累积回报**（RL）|
> |3.2|**怎么把参数定下来？**|见下，五种|
> |3.3|有没有内环？|无／**每步需采样**（玻尔兹曼机的 CD）／每步需 rollout（RL）|
> |3.4|何时停？|收敛判据／**预算与验证曲线（早停：故意不让它收敛）**／不适用|
> |3.5|规模与预算怎么分？|算力给参数量／数据量／训练步数。**目标规模上跑不起搜索，只能外推**|
>
> ### 3.2 的五种取值
>
> |取值|例子|
> |---|---|
> |**闭式解**|OLS、岭回归|
> |**一次代数运算**|PCA（特征分解）|
> |**迭代优化**|凸（逻辑回归、SVM 的 QP）／非凸（SGD、Adam、BPTT）|
> |**积分而非优化**|高斯过程与贝叶斯方法：没有点估计 $\theta$，只有后验|
> |**一次写入**|Hopfield 的赫布规则|
> |**随求解生长**|GBDT 的逐轮加法与贪心分裂（与 2.3 联动）|
>
> **这一格是整张表信息量最大的地方。**它也解释了为什么"训练"不能当节点名 —— 训练只是其中一个取值。
>
> ### 3.2 取"迭代优化"时才出现的两条性质
>
> 以下两条**不是节点的性质**，只挂在这一个取值下面。
>
> **（a）双重时间。** 深度方向 $h_{\ell+1}=h_\ell+F(h_\ell;\theta_\ell)$ 的时间是层号 $\ell$；训练方向 $\theta_{t+1}=\theta_t-\eta g_t$ 的时间是迭代数 $t$。两者不同源。加上推理时算法（4.x）严格说还有第三个。 注意深度那条写法**长得像差分方程但里面没有未知量** —— 给定 $h_0$ 直接算出 $h_L$，没有 $\Delta\ell\to 0$ 的极限是我们想要的。
>
> **（b）离散性有益，判据反转。** $\theta_{t+1}=\theta_t-\eta g_t$ 是梯度流的显式欧拉格式，edge of stability（$\eta\lambda_{\max}\approx 2$）就是显式欧拉的稳定性边界，与 CFL 条件同一个数学。但：
>
> ||数值积分|深度学习|
> |---|---|---|
> |步长|误差来源，应减小|**$\eta\to0$ 模型更差**|
> |噪声|应抑制|SGD 噪声有益|
> |两个都收敛的格式|一样好，差别叫误差|**给出不同模型，差别叫隐式正则化 —— 唯一在意的东西**|
>
> 即：数值格式不是对某个真实解的近似，**格式本身就是那个对象**。
>
> 3.3 是全部人力所在（发散、loss spike、平台期 → 回到第二块换旋钮，没有原则告诉你换哪个，所以扫参数）。 3.5 是同一困难的极端版：扫不起，只能预测。
>
> ## 四　检验
>
> |节点|问题|可能的取值|
> |---|---|---|
> |4.1|拿什么比？|留出集／基准／人评／配对比较／红队|
> |4.2|**有没有外部真值？**|**通常没有**（同一个 $P$ 的样本）／**有**（AlphaFold：晶体结构由实验测定，CASP 盲测）|
> |4.3|上线后分布变不变？|不变／漂移／**输出回流进训练数据，$P$ 不再独立于模型**|
> |4.4|懂不懂自己造了什么？|能力边界、机制可解释性 —— **通常为空，且缺位不阻碍前三块完成**|
>
> 推理时算法（温度、CoT 长度、工具、best-of-n）意味着系统行为不只由 $\theta$ 决定 —— 归在 4.1 之前还是单列，见未决。
>
> ## 覆盖性验证
>
> 十六个模型，逐个填关键节点。无空格、无例外。
>
> |模型|2.1 函数族|2.4|3.1 判据|**3.2 怎么定参数**|4.2 外部真值|
> |---|---|---|---|---|---|
> |逻辑回归|线性＋sigmoid|通路|交叉熵|迭代（凸）|无|
> |OLS／岭回归|线性|通路|平方误差|**闭式**|无|
> |PCA|线性子空间|通路|重构误差|**特征分解**|无|
> |SVM|核 RKHS|通路|间隔|**凸 QP（对偶）**|无|
> |k-NN|无参数|通路|—|**不求解**（训练集即模型）|无|
> |树／GBDT|分段常数|通路|任意可微损失|**生长**（贪心分裂＋逐轮加法）|无|
> |高斯过程|核定的先验|分布|边际似然／后验|**积分，非优化**|无|
> |Hopfield|对称权重能量|分布|存储容量|**一次赫布写入**|无|
> |玻尔兹曼机|能量函数|**分布**|似然|迭代，**内环要 MCMC**|无|
> |CNN／AlexNet|卷积堆叠|通路|交叉熵|迭代（SGD）|无|
> |RNN|循环|通路|交叉熵|迭代（BPTT）|无|
> |Transformer|注意力堆叠|通路|下一 token|迭代（Adam）|无|
> |扩散模型|去噪网络|分布|score matching|迭代，**内环无采样**|无|
> |RL|策略／价值网|通路|**累积回报**|迭代（半梯度／策略梯度）|无；**1.3 在环里**|
> |GAN|生成器＋判别器|分布|**均衡，非极小**|交替更新|无|
> |AlphaFold|等变＋注意力|通路|结构误差|迭代|**有：CASP 盲测**|
>
> 伊辛模型与玻尔兹曼方程不在表内 —— 前者是物理模型（玻尔兹曼机借了它的形式），后者是动理学方程。玻尔兹曼方程另有用处，见下节。
>
> ## 核心诊断：没有「方程」这一层
>
> 一个方程要：先于解法存在、独立于解法、**且可以是错的**。逐层查：
>
> |层|成熟科学|ML|
> |---|---|---|
> |微观运动方程|有，是关于对象的可错猜想|**没有。有的是算法** —— 2.1 与 3.2 都是我们自己规定的，无可证伪|
> |变分原理|有，且**经欧拉-拉格朗日转成方程**|有（3.1），但**从不转成方程**，直接进 3.2 硬算|
> |数值格式|对方程的近似，误差应减小|**格式本身就是对象**（见 3.2b）|
> |宏观状态方程|有，且可从微观导出|**有 —— 标度律。但纯经验，未被导出**|
>
> **参照物：玻尔兹曼方程。**从刘维尔方程出发，经 BBGKY 层级，用分子混沌假设做闭合，得到宏观方程 —— 这是"方程层"的教科书示范，正是 ML 缺的那一层长什么样。
>
> **推论。**一切降维手段（微扰、平均场、变分、标度分析）都作用于方程。没有方程就没有可作用的对象。ML 目前只有两头：底下是算法，顶上是拟合的状态方程，中间空。
>
> > **缺的不是"近似层"，缺的是"方程层"。近似层的缺失是推论。**
>
> ### 一个坏消息
>
> 看覆盖表的 2.4 与 3.2 两列：**真有"方程层"的那一支（玻尔兹曼机、Hopfield、能量模型）和精确可解的那一支（OLS、PCA、SVM、高斯过程），全部边缘化了。**它们恰好是配分函数、replica、随机矩阵、凸对偶最好用的地方。
>
> 反相关是真的。而扩散模型是唯一反例 —— 它满身物理却赢了。差别在 **3.3**：玻尔兹曼机每步梯度都要 MCMC，扩散模型用 score matching 绕开了 $Z$，训练变成纯回归。
>
> > 输掉的可能不是"有方程"，而是"**内环里要采样**"。
>
> ## 物理叠加位（留空）
>
> 逐格问：这个节点的困难是什么**类型**的困难？有没有已有的知识体系专门处理这类困难？
>
> 预留：1.1–1.4｜2.1–2.4（2.1 与 2.2 成色可能差别极大）｜3.1–3.5，及 3.2 的六个取值与 (a)(b) 两条性质｜4.1–4.4｜核心诊断四行｜内环判据
>
> ## 未决
>
> 1. 1.3 里的表征（tokenizer）其实是构造，是否移进第二块？
> 2. 推理时算法单列成节点还是并入 4.1？它意味着考察对象不只是 $\theta$。
> 3. 4.3／4.4 是否属于本文范围？
> 4. 核心诊断第四行需定调：若承认标度律算方程，则 ML 是**有宏观方程而缺微观方程**，与物理正好相反 —— 更强也更奇怪，但可能是对的。
> 5. 「内环判据」是当核心诊断的补充，还是升格为全文主张（_物理的工具最适用之处恰是 ML 放弃之处，出路是找出无需内环采样的物理结构_）？后者更锋利，但把全文从描述性变成论战性。
> 6. 4.2 = 有（AlphaFold）要不要单独成节：若"无外部真值"是结构性缺陷，它就是唯一的存在性证明。
>
> # 文章提纲
>
> > **以上全部为素材层，不动。**以下是把素材扩成一篇独立文章的结构。
> >
> > **范围决定**：正文只写 ML 自身，**物理不进正文**，留作续篇。 理由：素材本身已足够独立成文；掺入物理会让论证从「这是 ML 的结构」滑成「物理该怎么用」，两件事的举证责任完全不同。
>
> ## 第一章　引论：为什么需要一张节点表
>
> |节|要说什么|
> |---|---|
> |1.1|现象：ML 有教科书流水线，却没有一张能同时装下 PCA 和 Transformer 的表|
> |1.2|现有三种写法及其不足：**按模型分类**（并列，无流程）／**按任务分类**（应用导向）／**按流水线**（隐含"可微模型＋梯度训练"）　【需补：真去查文献，目前凭印象】|
> |1.3|本文要的东西：一张对全部 ML 成立的节点表，以及从表里读出来的结论|
> |1.4|结论预告：表的重心在「怎么把参数定下来」这一格；由此读出 ML 缺「方程层」|
>
> ## 第二章　方法：节点是问题，不是答案
>
> |节|要说什么|
> |---|---|
> |2.1|区分：**节点＝必须回答的问题**；**取值＝某类模型给出的答案**|
> |2.2|反面演示：把第三块写成「推进」，PCA／k-NN／高斯过程立刻"不符合"　【素材：文件顶部写法约定 —— 用本文自己的失败版本当例子】|
> |2.3|合格判据：全部模型都能填满，且无空格、无例外|
> |2.4|与分类学的区别：节点表不给类别，只给待填的格子|
>
> ## 第三章　节点清单
>
> |节|要说什么|
> |---|---|
> |3.1|定题（1.1–1.4）；重点 **1.4 目标与数据谁先**，自监督如何把顺序倒过来|
> |3.2|构造（2.1–2.4）；重点 **2.3 固定 vs 生长**、**2.4 通路 vs 分布**|
> |3.3|求解（3.1–3.5）—— 只列出，深挖放第四章|
> |3.4|检验（4.1–4.4）；重点 **4.2 外部真值**|
> |3.5|符号与可观测量：$W\to M\to L$ 三层代理；真正在意的 $\varepsilon$ 不可计算|
>
> ## 第四章　求解：整张表的重心
>
> |节|要说什么|
> |---|---|
> |4.1|3.2 的六种取值：闭式／代数一次／迭代／积分／一次写入／随求解生长|
> |4.2|为什么「训练」不能当节点名 —— 它只是六个取值之一|
> |4.3|迭代优化独有性质（a）：双重时间；深度那条写法为何不是方程|
> |4.4|迭代优化独有性质（b）：离散性有益、判据反转（CFL ↔ edge of stability）|
> |4.5|内环（3.3）：有无内环采样是一条**独立于 3.2** 的轴|
> |4.6|停止（3.4）：早停意味着故意不收敛|
> |4.7|规模（3.5）：扫不起 → 必须外推；软负担升级为硬约束|
>
> ## 第五章　覆盖性检验
>
> |节|要说什么|
> |---|---|
> |5.1|十六个模型逐节点填表|
> |5.2|边界案例：伊辛模型、玻尔兹曼方程为何不在表内|
> |5.3|从表里读出的经验规律：3.2 的取值分布／2.4 与 3.3 的联动／4.2 几乎恒为「无」|
> |5.4|反例征集：还有什么能打破这张表　【需补：上下文学习、神经符号、程序合成、贝叶斯非参、检索增强】|
>
> ## 第六章　诊断：没有「方程」这一层
>
> |节|要说什么|
> |---|---|
> |6.1|方程的三条标准：先于解法／独立于解法／**可以是错的**|
> |6.2|四层逐一查：微观运动方程／变分原理／数值格式／宏观状态方程|
> |6.3|参照物：玻尔兹曼方程如何从刘维尔经 BBGKY 闭合出来 —— 「方程层」的样本|
> |6.4|推论：一切降维手段作用于方程；无方程即无可降维之物|
> |6.5|待定调：标度律算不算方程（若算 → ML 有宏观而缺微观，与物理相反）|
>
> ## 第七章　一个反相关与一个判据
>
> |节|要说什么|
> |---|---|
> |7.1|反相关：可解的那一支与有配分函数的那一支，全部边缘化|
> |7.2|反例：扩散模型满身物理却赢了|
> |7.3|差别在内环：CD 的每步采样 vs score matching 绕开 $Z$|
> |7.4|判据：输掉的不是「有方程」，是「**内环要采样**」|
> |7.5|这条判据如何被证伪；拿什么去检验它|
>
> ## 第八章　边界与未完成
>
> |节|要说什么|
> |---|---|
> |8.1|本文没做的：没解释注意力为何有效 —— 2.1 那一格本文只列不解释|
> |8.2|4.4（理解）恒为空这件事本身值得单独说|
> |8.3|续篇接口：物理叠加位 —— 逐节点问「困难是什么类型」|
>
> ## 写作前必须先定的
>
> 沿用上面「未决」1–6，另加三条：
>
> 7. **1.2 那一节要不要真查文献。**不查则第一章立不住（"没人做过"是空话）；查则要花时间，且可能发现已有人做过类似的表。
> 8. **第七章升格与否，决定全文性质。**留作附论 → 描述性文章；升为主张 → 论战性文章（_物理工具最适用之处恰是 ML 放弃之处_）。两者的举证标准差一个量级。
> 9. **篇幅与投向。**第三、五章是表格密集型，第六、七章是论证型。若投综述类，前者为主；若投观点／立场类，后者为主，第三五章压缩成附录。
